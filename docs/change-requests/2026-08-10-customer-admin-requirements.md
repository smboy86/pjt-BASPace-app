# 2026-08-10 고객·관리자 수정 요구사항 및 실행 TODO

> 상태: 요구사항 확정·단계별 구현 중
>
> 기준 문서: `docs/screen-definition.md`
>
> 원칙: 이 문서는 접수 내용과 실행 순서를 관리한다. 화면의 최종 동작은
> `docs/screen-definition.md`에 확정된 뒤 구현한다.

## 1. 목적

성윤 측에서 전달한 고객 화면, 관리자 화면, 견적 카테고리 수정사항을 누락 없이 정리하고,
결정이 필요한 항목과 외부 자료 의존성을 분리하여 한 항목씩 개선할 수 있는 작업 목록으로
전환한다.

## 2. 요구사항 정리

### 2.1 고객 기본 인터페이스

#### 브랜드와 UI/UX

- 앱 전반의 메인 컬러를 네이비 계열로 변경해 신뢰감 있는 브랜드 인상을 제공한다.
- 고객 하단 내비게이션은 `견적요청 → 나의견적 → 홈 → 알림 → 설정` 순서의 5개 탭으로
  구성하고, 가운데 홈은 탭 바 위로 돌출된 원형 버튼으로 표시한다. 무신사 앱의 중앙 홈
  배치는 참고 사례이며 Safe Area와 접근성 기준을 함께 적용한다.
- 별도 고객 홈은 이미지 중심의 랜딩 화면으로 구성한다.
- 홈의 메인 비주얼 이미지와 서비스 안내 문구는 성윤 측 제공 자료를 사용한다.
- 고객 로그인과 세션 복원 완료 후 기본 진입 화면은 홈으로 변경한다.
- Android에서 견적요청·나의견적·알림·설정의 탭 최상위 화면에서 뒤로가기를 누르면 홈으로
  이동한다. 상세·입력 화면은 직전 화면으로 돌아가고 이력이 없으면 홈으로 이동한다.
- Android 홈에서 첫 뒤로가기는 종료 안내를 표시하고, 2초 이내 두 번째 뒤로가기에서 앱을
  종료한다. 다른 탭에서 홈으로 이동한 입력은 종료 첫 입력으로 계산하지 않는다.

#### SNS 회원가입과 인증

- Google 로그인·회원가입을 우선 지원한다.
- 다른 SNS 공급자는 개발 소요 시간과 운영 필요성을 검토한 뒤 후속 범위로 결정한다.
- 기존 이메일 로그인·회원가입의 유지 여부와 Google 계정의 기존 고객 계정 연결 정책은
  구현 전에 확정한다.

### 2.2 욕실 공사 옵션과 프로세스

#### 시공 타입

- 견적 요청의 최상단에 `철거`와 `덧방` 선택을 배치한다.
- 두 값 중 하나를 반드시 선택하는 단일 선택 UI로 제공한다.
- 타일 선택과 무관한 공사 전체 상위 조건으로 저장한다. `철거`는 기존
  `requires_demolition = true`, `덧방`은 `false`에 대응한다.
- `철거`를 선택하면 방수 작업과 비용이 자동으로 견적에 포함되어야 한다.
- 자동 추가되는 방수 비용의 금액, 수량, 수정 가능 여부와 견적서 표시 방식은 운영 정책을
  받아 확정한다.
- 방수 작업 추가 금액과 적용 상태를 관리자가 관리할 수 있는 운영 화면을 제공한다. 화면 위치,
  가격 변경의 기존 견적 영향과 요청별 수정 권한은 후속 결정에서 확정한다.

#### 용어·카테고리·일정

- 기존 `수조` 명칭은 `수전`으로 변경한다.
- 견적 선택 항목에 `줄눈`을 추가한다.
- 고객이 선택한 공사 희망 날짜를 관리자가 추후 수정할 수 있게 한다.
- 고객은 캘린더에서 오늘 이후의 정확한 날짜 하나를 선택하고 `YYYY-MM-DD`로 저장한다.
- 관리자는 변경 사유 입력 없이 날짜를 직접 수정할 수 있다. 원래 날짜, 변경 날짜, 변경
  관리자와 변경 시각은 자동 기록하고 고객 화면에 변경 안내를 표시한다.
- 일정 변경 인앱 알림은 알림 기능 구현 시 연결하며 푸시 알림은 후속 범위다.

### 2.3 관리자 화면

| 영역 | 접수 내용 | 처리 상태 |
|---|---|---|
| 견적 관리 | 견적서 누락 제보 | 오류가 아닌 것으로 확인되어 작업 취소 |
| 업체 관리 | 기존 업체 관리 사양과 기능을 유지한다. | 변경 없음·회귀 검증 대상 |
| 견적 옵션 수정 | 수정 후 저장되지 않는다는 요청 | 오류가 아닌 것으로 확인되어 작업 취소 |

`견적 옵션 수정` 저장 건은 새 수정 작업을 만들지 않는다. 다만 다른 견적 옵션 변경 후 기존
저장 기능이 깨지지 않았는지는 회귀 테스트한다.

`견적서 누락` 제보도 오류가 아닌 것으로 확인하여 별도 진단·수정 작업을 진행하지 않는다.
향후 카테고리·방수 비용 변경에 대한 일반 견적 산출 검증은 종합 QA에서 수행한다.

### 2.4 고객 선택용 견적 상세 카테고리

최종 목표는 다음 8개 상위 항목이다.

| 순서 | 고객 노출 카테고리 | 세부 요구사항 |
|---:|---|---|
| 1 | 타일 | 타일 이미지 확대 기능 포함 |
| 2 | 세면대 / 양변기 | 제품 선택 |
| 3 | 수전 / 유가 | 기존 `수조` 용어 교체 및 유가 범위 포함 |
| 4 | 욕조 | 제품 선택 |
| 5 | 젠다이 / 조적 파티션 | 기존 분리 항목의 통합 여부 검토 후 반영 |
| 6 | 천장재 / 조명 / 환풍기 | 기존 천장·간접등과 신규 환풍기 범위의 통합 여부 검토 후 반영 |
| 7 | 줄눈 | 신규 카테고리 |
| 8 | 욕실장 | 제품 선택 |

원격 카탈로그는 목표 8개 마스터로 전환됐다. 유지 코드 4개의 기존 제품은 보존했고 신규 코드
4개는 제품이 없는 상태로 생성했다. 기존 5개 코드와 연결 제품은 private 백업을 남긴 뒤
삭제했으며, 요청·선택 스냅샷·견적 업무 데이터는 유지했다.

### 2.5 성윤 측 수급 필요 자료

| 자료 | 사용 위치 | 자료 수급 전 처리 |
|---|---|---|
| 최종 견적서 아웃풋 샘플 | 고객 제공 견적서 레이아웃·출력 양식 | 출력 기능과 필드 매핑 확정 보류 |
| 홈 메인 이미지 | 고객 홈 메인 비주얼 | 자리표시자 규격만 정의 가능 |
| 홈 서비스 설명 문구 | 고객 홈 안내 콘텐츠 | 최종 카피 반영 보류 |

추가로 시공 타입 자동 비용 구현을 위해 방수 작업의 기준 금액·과금 단위·관리자 수정 정책이
필요하다.

## 3. 선행 결정 목록

- [x] 고객 탭 순서, 중앙 돌출 원형 홈 버튼, 기본 진입 화면과 Android 뒤로가기 정책을
  확정한다.
- [x] 신뢰형 네이비 팔레트와 로고 사용 규칙을 확정한다. Primary `#163A63`, Dark
  `#0B1F3A`, Light `#E8EEF6`, Background `#F7F8FA`, Accent `#B86C22`를 사용하고,
  로고는 밝은 배경에 네이비·어두운 배경에 흰색을 사용한다.
- [x] 기존 이메일 가입·로그인을 변경 없이 유지하고 고객용 Google 인증만 추가한다. Google의
  인증 이메일과 같은 기존 계정이 있으면 자동 연결·중복 가입을 하지 않고 이메일 로그인을
  안내하며, 업체·관리자는 Google 로그인을 허용하지 않는다.
- [x] `철거/덧방`을 타일과 무관한 공사 전체 상위 필수 선택으로 확정하고 기존
  `requires_demolition`의 `true/false`로 저장한다.
- [x] 방수 작업은 관리자 `시공 비용 설정`의 활성 상태와 전역 기본 금액으로 관리하고 수량은
  요청당 `1식`으로 확정한다. 철거 요청 제출 시 금액을 스냅샷으로 보존하며, 관리자는 최초
  견적 조정 전 요청별 금액을 수정하고 변경 주체·시각·원래 금액을 기록한다.
- [x] 변경되는 영역은 신규 코드 `FAUCET_DRAIN`, `ZENDAI_PARTITION`,
  `CEILING_LIGHT_VENT`, `GROUT`를 사용하고 기존 `TILE`, `BASIN_TOILET`, `BATHTUB`,
  `BATHROOM_CABINET`은 유지한다. 기존 통합 대상 카탈로그 코드·제품을 먼저 삭제한 뒤 과거
  견적 로딩을 검증하고, 복구가 어렵게 깨지는 경우에만 과거 견적 업무 데이터를 삭제한다.
- [x] 고객은 오늘 이후의 정확한 공사 희망 날짜 하나를 선택하고 관리자는 사유 입력 없이
  오늘 이후 날짜로 직접 변경한다.
  원래 날짜·변경 날짜·관리자·변경 시각을 자동 보존하고 고객 화면과 향후 인앱 알림에
  변경 사실을 표시하며 푸시 알림은 후속 범위로 둔다.
- [x] `견적서 누락` 제보는 오류가 아닌 것으로 확인하고 관련 진단·수정 작업을 취소한다.

## 4. 단계별 실행 TODO

한 번에 하나의 체크 항목을 완료하고 검증한 뒤 다음 항목으로 이동한다.

### Phase 0. 요구사항 확정과 자료 수급

- [x] `P0-01` 선행 결정 8개를 확정하고 화면정의서에 반영한다.
- [ ] `P0-02` 홈 이미지와 서비스 설명 문구를 수급한다.
- [ ] `P0-03` 최종 견적서 샘플을 수급하고 필수 출력 필드를 표로 정리한다.
- [x] `P0-04` 견적서 누락 제보를 취소하고 방수 비용 운영·관리 정책을 확정한다.

완료 조건: 화면 구조, 데이터 전환, 인증, 자동 비용, 날짜 수정의 제품 규칙에 미결정 항목이
없다.

### Phase 1. 고객 홈과 중앙 홈 내비게이션

- [x] `P1-01` 탭 순서를 `견적요청 → 나의견적 → 홈 → 알림 → 설정`으로 확정하고, 홈을
  돌출 원형 중앙 버튼과 고객 기본 진입 화면으로 정의한다.
- [ ] `P1-02` 제공 이미지·문구를 사용한 이미지 중심 `CUS-010 고객 홈 랜딩`을 구현한다.
  현재는 자료 수급 전 콘텐츠 준비 상태 화면까지만 구현했다.
- [x] `P1-03` 중앙 홈 버튼의 선택·비선택, 돌출 레이아웃, Safe Area와 접근성 상태를 구현한다.
- [x] `P1-04` 고객 로그인·세션 복원 완료 후 `/(tabs)/home`으로 이동하도록 기본 진입을 변경한다.
- [x] `P1-05` 고객 역할 루트 판정을 `나의견적`에서 `홈`으로 변경하고, 견적요청·나의견적·
  알림·설정 탭 최상위에서 Android 뒤로가기 시 홈으로 이동한다.
- [x] `P1-06` 기존 `goBackOrCustomerQuotes`를 홈 fallback 책임이 드러나는
  `goBackOrCustomerHome`으로 교체한다. 상세·입력 화면은 직전 화면으로 돌아가고, 이력이
  없으면 홈으로 이동하게 한다.
- [x] `P1-07` 홈에서 첫 뒤로가기 시 종료 안내를 표시하고 2초 이내 두 번째 입력에서만 앱을
  종료한다. 다른 탭에서 홈으로 이동한 입력은 종료 첫 입력으로 계산하지 않는다.
- [ ] `P1-08` iOS 스택·제스처 유지, Android 2초 초과 초기화, 딥링크와 접근성을 검증한다.
  관련 단위 테스트·타입 검사·린트는 통과했으며 실제 iOS·Android 기기와 스크린 리더 검증은
  남아 있다.

완료 조건: 홈이 중앙 탭과 고객 기본 진입 화면으로 일관되게 열리고, 다른 탭·상세·홈의
Android 뒤로가기와 iOS 기존 내비게이션이 정의대로 동작한다.

### Phase 2. 견적 누락 진단 — 취소

- [x] `P2-01` 견적서 누락 제보가 오류가 아닌 것으로 확인되어 별도 재현·진단·수정 작업을
  진행하지 않는다.

처리 원칙: 별도 오류 작업은 종료하지만 Phase 3~4 변경 후 정상 견적 항목·합계 산출과 기존
업체 관리·옵션 저장 회귀 검증은 Phase 10 종합 QA에서 수행한다.

### Phase 3. 카테고리·용어 데이터 전환

- [x] `P3-01` 목표 8개 코드와 기존 데이터 정리 정책을 확정한다. 변경 영역은 신규 코드를
  사용하고 기존 카탈로그를 먼저 삭제하며, 과거 견적 삭제는 로딩 문제 발생 시에만 허용한다.
- [x] `P3-02` 기존 코드·제품·요청 스냅샷·견적 업무 데이터의 관계와 삭제 대상을 읽기 전용으로
  조사하고 [복구·검증표](./2026-08-10-category-migration-audit.md)를 작성했다.
- [x] `P3-03` 유지 코드 4개와 신규 `FAUCET_DRAIN`, `ZENDAI_PARTITION`,
  `CEILING_LIGHT_VENT`, `GROUT`를 생성하고 전환 전 데이터를 private 백업으로 보존하는
  Supabase migration `20260810000100`을 작성했다.
- [x] `P3-04` dry-run을 수행할 수 없는 환경임을 확인하고 사용자 승인에 따라 migration 내부
  사전 코드 검증·transaction 백업·사후 8개 코드 검증과 적용 후 원격 통계 확인으로 대체했다.
- [x] `P3-05` 승인된 카탈로그 migration을 원격에 적용했다. public 스키마 계약은 변경되지 않아
  DB 타입과 데이터 기반 관리자·고객 API 수정은 필요하지 않았으며 linked DB lint를 통과했다.
- [x] `P3-06` 과거 고객 요청·선택·견적 상세의 추가 실제 로딩 검증은 사용자 결정으로
  생략한다. 운영 중 문제가 확인되면 보존된 스냅샷과 private 백업을 기준으로 다시 검토한다.
- [x] `P3-07` 현재 과거 업무 데이터는 삭제하지 않는다. 운영 중 복구 불가능한 로딩 문제가
  확인될 때만 사용자와 삭제 범위를 다시 협의한다.

완료 조건: 고객과 관리자가 목표 8개 카테고리를 같은 순서와 명칭으로 보고, 과거 견적은 정상
로드되거나 검증된 문제 데이터만 관계 무결성을 지켜 정리되어야 한다.

### Phase 4. 시공 타입과 방수 자동 비용

- [x] `P4-01` 상위 필수 시공 타입, 기존 `requires_demolition`, 관리자 전역 기본 금액,
  요청당 `1식` 스냅샷과 최초 견적 조정 전 요청별 수정·감사 이력 정책을 확정한다.
- [ ] `P4-02` 설정·요청 스냅샷·수정 이력을 위한 DB 스키마와 서버 계산 계약을 설계한다.
- [ ] `P4-03` 관리자 운영 관리에 `ADM-009 시공 비용 설정` 화면과 저장 권한을 구현한다.
- [x] `P4-04` 견적 요청 최상단에 `철거/덧방` 단일 선택 UI와 검증을 구현했다. 제출 RPC
  migration을 원격에 적용해 `requires_demolition`으로 저장한다.
- [ ] `P4-05` 활성 방수 설정이 있고 철거를 선택하면 제출 시점 기본 금액을 요청에 자동
  스냅샷으로 저장한다.
- [ ] `P4-06` 관리자가 최초 견적 조정 전 요청별 방수 금액을 수정하고 변경 이력을 남기도록
  `ADM-003`을 보완한다.
- [ ] `P4-07` 선택 변경 시 중복·잔여 비용을 방지하고 고객 상세, 관리자 상세와 최종 견적서에
  같은 작업명·수량·금액을 표시한다.

완료 조건: 클라이언트 표시 합계와 서버 저장·산출 합계가 일치하고, 기존 요청 금액 보존,
철거·덧방 전환, 설정 비활성, 요청별 수정 권한과 감사 이력 테스트가 통과한다.

### Phase 5. 옵션 제품 이미지 확대

- [x] `P5-01` 이미지가 있는 모든 고객 옵션 카드의 이미지 우하단에 상세보기 아이콘을 두고,
  카드 선택과 분리된 전체 화면 확대 보기를 구현한다.
- [x] `P5-02` 닫기, Android 뒤로가기, 확대 이미지 로딩·오류 상태와 접근성 레이블을 적용한다.
- [ ] `P5-03` Android development 기기의 기본 확대·닫기·선택 분리 동작은 사용자 확인을
  완료했다. iOS와 작은 화면, 세로·가로 이미지별 표시 검증은 남아 있다.

완료 조건: 확대 화면에서 이미지가 잘리지 않고, 키보드·탭·내비게이션 흐름을 깨지 않는다.

### Phase 6. 공사 희망 날짜 관리자 수정

- [x] `P6-01` 고객의 오늘 이후 정확한 날짜 1개 선택, 관리자 직접 변경, 사유란 없음, 원래 날짜·변경
  날짜·관리자·변경 시각 자동 기록과 고객 안내 정책을 확정한다.
- [ ] `P6-02` 정확한 `YYYY-MM-DD` 날짜와 변경 이력을 저장하고 관리자만 수정할 수 있도록
  마이그레이션, RLS/RPC와 서버 날짜 검증을 구현한다.
- [x] `P6-03` 고객 견적 요청에 기기 로컬 날짜 기준 내일부터 선택 가능한 캘린더와 필수 검증을
  구현했다. 제출 RPC migration을 원격에 적용해 `YYYY-MM-DD`로 저장한다.
- [ ] `P6-04` 관리자 견적 상세에 사유 입력 없는 날짜 수정 UI와 저장 상태를 구현한다.
- [ ] `P6-05` 고객 목록·상세에 현재 날짜와 관리자 변경 안내를 반영하고, 알림 모델 구현 시
  일정 변경 인앱 알림을 연결한다.
- [ ] `P6-06` 고객 변경 차단, 관리자 변경 성공, 날짜 이력과 재접속 복원을 인증 역할 E2E로
  검증한다.

완료 조건: 날짜와 자동 변경 이력이 서버에 저장되고 재접속 후 유지되며, 고객 권한으로는
변경할 수 없고 화면에 사유 입력을 요구하지 않는다.

### Phase 7. 네이비 디자인 시스템

- [x] `P7-01` 신뢰형 네이비 브랜드 팔레트와 로고 사용 규칙을 확정한다. 타이포그래피,
  세부 상태색과 명암 대비 검증은 구현 단계에서 보완한다.
- [ ] `P7-02` 디자인 토큰과 공통 UI를 변경하고 임의 하드코딩 색상을 정리한다.
- [ ] `P7-03` 인증·고객·업체·관리자 전체 화면의 시각 회귀와 접근성을 점검한다.

완료 조건: 화면별 임의 색상 없이 공통 토큰을 사용하고, 주요 텍스트·버튼의 명암 대비 기준을
충족한다.

### Phase 8. Google 인증

- [x] `P8-01` 이메일 인증 유지, 고객용 Google 인증 추가, 동일 이메일 자동 연결·중복 가입
  금지와 업체·관리자 Google 로그인 차단 정책을 확정한다.
- [ ] `P8-02` Google Cloud, Supabase Auth, iOS·Android OAuth 설정과 리디렉션 정책을 확정한다.
- [ ] `P8-03` Google 로그인·회원가입 UI와 취소·실패·동일 이메일 안내 상태를 구현한다.
- [ ] `P8-04` 세션을 기존 SecureStore 경계 안에서 저장하고 신규 Google 고객 프로필 생성
  규칙을 적용한다.
- [ ] `P8-05` 신규 Google 고객, 기존 이메일 고객 안내, 로그아웃·재로그인과 역할 차단을 실제
  인증으로 검증한다.

완료 조건: 기존 이메일 인증에 회귀가 없고 신규 Google 고객 흐름이 실제 기기에서 동작하며,
동일 이메일 중복 계정과 업체·관리자 Google 로그인이 생성되지 않는다.

### Phase 9. 최종 견적서 출력

- [ ] `P9-01` 수급한 샘플을 기준으로 필드·카테고리·금액·날짜 매핑을 확정한다.
- [ ] `P9-02` 출력 생성 방식과 저장·공유·개인정보 노출 범위를 결정한다.
- [ ] `P9-03` 고객용 최종 견적서 화면 또는 파일 출력을 구현한다.
- [ ] `P9-04` 긴 제품명, 빈 이미지, 할인·부가세, 8개 카테고리, 방수 자동 비용을 검증한다.

완료 조건: 승인된 샘플과 필수 필드가 일치하고 누락·합계 오차 없이 고객에게 제공할 수 있다.

### Phase 10. 종합 QA

- [ ] `P10-01` 업체 관리 기존 사양을 회귀 테스트한다.
- [ ] `P10-02` 견적 옵션 수정·저장이 정상임을 회귀 테스트한다.
- [ ] `P10-03` 역할별 화면, 권한, 내비게이션, 로딩·빈 상태·오류 상태를 점검한다.
- [ ] `P10-04` 화면정의서 검증, typecheck, lint, 테스트를 모두 통과시킨다.
- [ ] `P10-05` iOS·Android 실제 기기에서 인증·이미지·날짜·견적 출력을 점검한다.

완료 조건: 프로젝트 하드 임계값을 모두 통과하고 남은 물리 기기·인증 E2E 범위를 명시한다.

## 5. 권장 진행 순서

`Phase 0 → Phase 1 → Phase 2 → Phase 3 → Phase 4 → Phase 5 → Phase 6 → Phase 7 → Phase 8 → Phase 9 → Phase 10`

고객 홈·내비게이션을 첫 구현 단계로 진행한다. Phase 1 착수 전 홈 이미지와 설명 문구를
수급하며, 수급이 지연되면 라우트·탭·뒤로가기 계약부터 구현하고 임시 콘텐츠는 운영 콘텐츠로
간주하지 않는다. Phase 2 견적 누락 진단은 취소되었으므로 건너뛰고 카테고리·산출 로직을
순서대로 처리한다. 최종 견적서 출력은 샘플과 새 카테고리·방수 비용이 확정된 뒤 진행한다.
