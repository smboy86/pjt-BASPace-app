# 바스페이스 통합 기획 및 화면정의서

> 문서 버전: 2.4
>
> 최종 갱신일: 2026-07-29
>
> 관리 범위: 공통 · 고객 · 업체 담당자 · 관리자 화면
>
> 정본 원칙: 화면, 기능, 버튼, 권한, 상태, 내비게이션에 관한 결정은 이 문서를 기준으로 한다.

## 1. 문서 목적

이 문서는 바스페이스 앱의 제품 기획과 화면 정의를 한곳에서 확인하기 위한 단일 정본이다.
새 기능을 개발하거나 기존 기능을 수정할 때 구현 전에 관련 정의를 확인하고, 구현이 끝나면
실제 동작에 맞게 이 문서를 함께 갱신한다.

`_workspace/plan/prd.md`, `_workspace/design/design-system.md`, `docs/specs/`는 배경 자료와
작업 이력을 제공한다. 화면 관련 내용이 서로 다르면 이 문서를 우선하며, 아직 결정되지 않은
내용은 임의로 현재 기능처럼 기록하지 않고 `계획` 또는 `검토 필요`로 표시한다.

### 1.1 이 문서만으로 확인할 수 있어야 하는 정보

- 앱의 목적, 사용자 역할, 핵심 업무 흐름
- 역할별 전체 화면 목록과 구현 상태
- 각 화면의 목적, 진입 조건, 주요 표시 정보
- 버튼·입력·선택 요소의 동작과 이동 경로
- 로딩·빈 상태·오류·완료·권한 제한 상태
- 실제 서버 연동 여부와 로컬 프로토타입 여부
- 아직 구현되지 않은 화면과 다음 개발 범위

### 1.2 상태 표기

| 상태 | 의미 |
|---|---|
| `구현` | 현재 코드에 화면과 핵심 동작이 존재한다. |
| `부분 구현` | 화면은 존재하지만 일부 역할, 데이터, 상태 또는 동작이 빠져 있다. |
| `로컬 구현` | 화면과 동작은 존재하지만 서버에 저장되지 않고 메모리 상태만 사용한다. |
| `계획` | 제품 범위에는 포함되지만 현재 화면 코드가 없다. |
| `보류` | 방향 결정이나 별도 승인이 필요하다. |

## 2. 제품 정의

### 2.1 한 줄 설명

바스페이스는 고객의 욕실 조건·사진·제품 선택을 구조화하고, 관리자 배정 후 고객과 업체
담당자가 여러 차례 견적과 코멘트를 주고받을 수 있게 하는 욕실 리모델링 견적 협업 앱이다.

### 2.2 핵심 범위

- 고객 이메일 가입·확인·로그인
- 고객의 욕실 견적 요청 작성과 사진 선택
- 관리자의 업체 및 특정 담당자 배정
- 업체 담당자의 견적 작성·수정 버전 발송
- 고객과 업체 담당자의 요청별 상담 타임라인
- 고객의 최종 견적 컨펌
- 업체 담당자 또는 관리자의 시공 완료 처리
- 관리자의 업체·카탈로그·가격표 관리

### 2.3 비범위

- 앱 내 결제와 계약금
- 전자계약과 세금계산서
- 공개 업체 디렉터리와 리뷰 랭킹
- AR·3D 욕실 렌더링
- 전체 시공 일정·공정·A/S 관리
- 광고, 구독, 인앱결제, 보상형 잠금 해제

### 2.4 역할과 권한

| 역할 | 코드 | 주요 권한 | 현재 앱 지원 상태 |
|---|---|---|---|
| 고객 | `customer` | 가입, 요청 작성, 요청·견적 열람, 코멘트, 최종 컨펌 | 인증 구현, 요청·상담은 로컬 구현 |
| 업체 담당자 | `partner_staff` | 배정 견적 열람, 진행·취소 응답, 시공 완료, 코멘트, 견적 버전 작성·발송 | 인증·업체 연결·배정 견적 조회와 진행·완료 응답 구현     |
| 관리자      | `admin`         | 요청 모니터링, 업체·담당자 배정, 시공 완료, 업체 승인, 카탈로그 관리   | 인증, 전체 요청 조회·완료 처리, 업체·카탈로그 관리 구현 |

인증 API는 활성 상태의 `customer`, `partner_staff`, `admin` 프로필을 허용한다.
`customer`는 고객 탭, `partner_staff`는 업체 담당자 홈, `admin`은 관리자 운영 홈으로
진입한다. 공개 회원가입은 항상 `customer` 프로필만 생성하며 관리자와 업체 담당자는
운영자가 생성·연결한다.

### 2.5 핵심 업무 흐름

```text
고객 가입 및 이메일 확인
→ 고객 로그인
→ 견적 요청 작성 및 제출
→ 관리자 초기 견적 금액 조정
→ 고객 조정 견적 확인
→ 관리자 업체·담당자 배정
→ 업체 담당자 배정 견적 확인 및 진행 또는 취소
→ 업체 담당자 최초 견적 발송
→ 고객 질문 또는 수정 요청
→ 업체 담당자 수정 견적 발송
→ 고객 최종 컨펌
→ 오프라인 계약·시공 상담으로 전환
```

`최종 컨펌`은 계약 또는 결제를 의미하지 않는다.

### 2.6 견적 요청 상태

| 상태 | 화면 표시 | 의미 | 변경 주체 |
|---|---|---|---|
| `draft` | 작성 중 | 고객이 요청을 작성하는 상태 | 고객 |
| `submitted` | 매칭 대기 | 요청 제출 후 관리자 배정을 기다리는 상태 | 고객 |
| `quote_adjustment` | 견적 조율 | 관리자가 초기 견적 금액을 조정하고 고객 확인 또는 업체 배정을 기다리는 상태 | 관리자·고객 |
| `matched` | 업체 매칭 완료 | 업체 또는 담당자가 배정된 상태 | 관리자 |
| `in_consultation` | 견적 협의 중 | 코멘트 또는 견적을 주고받는 상태 | 고객·업체 |
| `final_quote_sent` | 최종 견적 도착 | 업체가 최종 견적을 발송한 상태 | 업체 |
| `confirmed` | 최종 컨펌 | 고객이 최종 견적을 확인한 상태 | 고객 |
| `closed`           | 시공 완료      | 배정 업체가 승인한 시공중 견적의 시공이 완료된 상태                         | 관리자·업체 |
| `cancelled` | 취소 | 배정 업체가 진행 불가를 확정해 견적 진행이 종료된 상태 | 업체 |

## 3. 전체 화면 요약

### 3.1 공통

| ID | 화면 | 경로 | 상태 | 주요 기능 |
|---|---|---|---|---|
| COM-001 | 앱 시작·세션 확인 | `/` 및 루트 레이아웃 | 구현 | 세션 복원, 고객·업체 담당자·관리자 역할별 자동 이동, 재시도 |
| COM-002 | 로그인 | `/(auth)/login` | 부분 구현 | 고객·업체 담당자·관리자 이메일 로그인, 개발용 임시 계정 입력, 고객 가입 이동 |
| COM-003 | 접근 제한 | `/(tabs)/assignment` | 구현 | 허용되지 않은 역할의 접근 차단과 이전 화면 복귀 |
| COM-004 | 전역 오류 대체 화면 | 전역 경계 | 부분 구현 | 예기치 않은 렌더링 오류 안내와 화면 재시도 |
| COM-005 | 견적 상세·협업      | 고객·업체·관리자 요청 상세 경로 | 구현      | 요청·선택 상세, 역할별 코멘트·진행 응답·견적 조정·조정 견적 확정·시공 완료   |

### 3.2 고객

| ID | 화면 | 경로 | 상태 | 주요 기능 |
|---|---|---|---|---|
| CUS-001 | 고객 회원가입 | `/(auth)/signup` | 구현 | 이름·이메일·비밀번호·필수 동의, 가입 요청 |
| CUS-002 | 이메일 확인 안내 | `/(auth)/verify-email` | 구현 | 확인 절차 안내, 확인 이메일 재전송, 로그인 이동 |
| CUS-003 | 나의 견적 | `/(tabs)` | 부분 구현 | 고객 요청 서버 조회, 로컬 최신 견적 여부, 견적 요청 이동 |
| CUS-004 | 견적 요청 | `/(tabs)/explore` | 구현 | 주소·예산·사진·관리자 제품 선택·추가 요청 제출 |
| CUS-005 | 고객 견적 상세 진입 | `/(tabs)/request/[requestId]` | 부분 구현 | COM-005 고객 모드, 코멘트, 조정 견적 확정 |
| CUS-007 | 설정 | `/(tabs)/settings` | 부분 구현 | 로그인 정보, 공지사항, 로그아웃, 앱스토어 이동 |
| CUS-008 | 알림 | `/(tabs)/notifications` | 부분 구현 | 알림 제목과 빈 상태 안내 |
| CUS-009 | 로그인 정보         | `/(tabs)/account`             | 구현      | 이메일 확인, 이름·연락처 조회 및 수정                    |

### 3.3 업체 담당자

| ID | 화면 | 예정 경로 | 상태 | 주요 기능 |
|---|---|---|---|---|
| PAR-001 | 배정 견적서 확인 | `/(partner)/requests` | 구현 | 배정·시공중·완료·취소 4개 탭, 배정된 견적만 조회, 상세 이동 |
| PAR-002 | 견적 상세        | `/(partner)/requests/[requestId]`           | 구현 | 고객 요청 리포트, 견적 확인 및 진행·진행 불가·시공 완료, 상담 타임라인 |
| PAR-003 | 견적 작성·수정 | `/(partner)/requests/[requestId]/quote/new` | 계획 | 견적 항목·수량·단가·할인·세금·유효기간 작성 |
| PAR-004 | 업체 담당자 홈 | `/(partner)/dashboard` | 구현 | 업체 연결 정보, 배정 견적서 확인 이동, 로그인 계정 확인, 로그아웃 |

### 3.4 관리자

| ID | 화면 | 예정 경로 | 상태 | 주요 기능 |
|---|---|---|---|---|
| ADM-001 | 관리자 운영 홈 | `/(admin)/dashboard` | 구현 | 견적 및 시공 관리·운영 관리 메뉴, 관리자 정보, 로그아웃 |
| ADM-002 | 견적 관리 | `/(admin)/requests` | 부분 구현 | 전체 요청 조회, 5개 상태 탭 필터, 상세 이동, 새로고침 |
| ADM-003 | 관리자 견적 상세·조정 | `/(admin)/requests/[requestId]` | 구현      | 고객 선택 옵션·코멘트 조회, 견적 조정, 등록 업체·대표 담당자 선택·배정, 시공 완료 |
| ADM-004 | 업체 목록 | `/(admin)/partners` | 구현 | 전체 업체 수, 등록 업체 목록, 업체 추가·상세 이동 |
| ADM-005 | 견적 옵션 목록 | `/(admin)/catalog` | 구현 | 코드·이름·타입·등록 제품 수 기준 카테고리 목록, 상세 이동 |
| ADM-006 | 업체 추가 | `/(admin)/partners/new` | 구현 | 업체·담당자·로그인 계정 입력, 증빙 첨부, 중복 검사, 업체·Auth 사용자 등록 |
| ADM-007 | 업체 상세 | `/(admin)/partners/[partnerId]` | 구현 | 등록한 업체·담당자·로그인 이메일·사업자등록증·비고 열람 |
| ADM-008 | 견적 옵션 상세·수정 | `/(admin)/catalog/[optionId]` | 구현 | 카테고리 속성과 복수 제품(이름·이미지·단가) 수정 |

### 3.5 현재 실행 가능한 범위

```text
고객 회원가입·이메일 확인·로그인
→ Supabase 견적 요청 제출
→ Supabase 나의 견적 목록과 요청·선택 상세 확인 및 상담 코멘트 작성
→ 관리자 조정 견적 확인

관리자 로그인
→ 관리자 운영 홈
→ 전체 고객 견적 요청 상태별 조회
→ 신규 견적 가격 조정
→ 고객 확인 후 업체 배정
또는 업체 목록 조회 → 업체 추가 또는 업체 상세 확인
또는 견적 옵션 목록 조회 → 견적 옵션 상세 수정

업체 담당자 로그인
→ 업체 담당자 홈
→ 배정 견적서 확인
→ 배정 견적 상세에서 진행 또는 취소 응답
→ 시공중 견적의 시공 완료 처리

관리자
→ 견적 진행 상세에서 시공 완료 처리
```

| 기능 영역 | 현재 데이터 연동 |
|---|---|
| 고객 가입·로그인·이메일 재전송·세션 복원·로그아웃 | 실제 Supabase 및 SecureStore |
| 요청 생성·목록·선택 상세 | 실제 Supabase 및 TanStack Query, 화면 협업 상태는 Zustand 인메모리 |
| 사진 | 기기 로컬 URI, 원격 업로드 없음 |
| 상담 메시지                                       | 실제 Supabase 및 TanStack Query                                           |
| 견적 버전·컨펌 | Zustand 인메모리, 견적 생성 UI 없음 |
| 업체 배정                                         | 실제 Supabase 관리자 선택 모달·RPC 및 TanStack Query                      |
| 업체 진행·취소 응답과 시공 완료                   | 실제 Supabase 제한 RPC 및 TanStack Query                                  |
| 관리자 견적 관리 | 실제 Supabase 전체 요청·고객·배정 업체 조회 및 TanStack Query |
| 업체 관리 | 실제 Supabase 목록·등록·상세 및 비공개 Storage 사업자등록증 |
| 업체 담당자 계정 | 관리자 전용 Edge Function에서 Auth 사용자·프로필·업체 대표 멤버 연결 |
| 견적 옵션 관리 | 실제 Supabase 카테고리 목록·상세 수정 및 카테고리별 복수 제품 이미지 관리 |
| 앱 재시작 후 요청 복원 | Supabase에서 고객 본인의 요청·선택 스냅샷 재조회 |
| 앱 재시작 후 견적 버전 복원                       | 지원하지 않음                                                             |
| 역할별 진입 | 고객·업체 담당자·관리자 지원 |
| 회원 탈퇴 | 미구현 |

업체 견적 버전 작성·발송, 실제 다회차 견적 수정과 최종 견적 도착은 현재 UI만으로
수행할 수 없다. 고객 요청부터 관리자 조정·배정, 업체 진행 승인과 시공 완료까지의 상태
전이는 실제 Supabase에 저장된다.

## 4. 공통 화면 정의

### COM-001 앱 시작·세션 확인

| 항목 | 정의 |
|---|---|
| 상태 | `부분 구현` |
| 구현 파일 | `app/index.tsx`, `app/_layout.tsx` |
| 대상 역할 | 전체 |
| 목적 | 앱 시작 시 저장된 Supabase 세션과 사용자 프로필을 확인하고 적절한 화면으로 보낸다. |
| 진입 | 앱 실행, 딥링크 진입, 세션 상태 변경 |
| 데이터 | Supabase Auth 세션, `profiles` 사용자 프로필, SecureStore 세션 저장소 |

주요 상태:

- 확인 중: 로딩 표시와 `로그인 정보를 확인하고 있어요.` 문구를 표시한다.
- 비로그인: 로그인 화면으로 교체 이동한다.
- 로그인: 프로필 역할에 따라 고객 탭, 업체 담당자 홈 또는 관리자 운영 홈으로 교체 이동한다.
- 확인 실패: 네트워크 안내와 `다시 시도` 버튼을 표시한다.

| 액션 | 조건 | 결과 |
|---|---|---|
| 다시 시도 | 세션 확인 오류 | 세션과 사용자 프로필을 다시 조회한다. |
| 자동 로그인 이동 | 유효 세션 없음 | `/(auth)/login`으로 이동한다. |
| 고객 홈 이동 | 활성 고객 세션 존재 | `/(tabs)`로 이동한다. |
| 업체 담당자 홈 이동 | 활성 업체 담당자 세션 존재 | `/(partner)/dashboard`로 이동한다. |
| 관리자 홈 이동 | 활성 관리자 세션 존재 | `/(admin)/dashboard`로 이동한다. |
| Android 앱 종료 안내 | 고객 탭 최상위 화면, 업체 담당자 홈 또는 관리자 운영 홈에서 하드웨어 뒤로가기를 처음 누름 | `한번 더 뒤로가기를 눌러 앱을 종료할 수 있어요` 토스트를 2초 동안 표시하고 앱을 유지한다. |
| Android 앱 종료 | 종료 안내 토스트를 표시한 뒤 2초 이내에 하드웨어 뒤로가기를 다시 누름 | 앱을 종료한다. 2초가 지나면 승인 상태를 해제하고 다음 뒤로가기에서 안내 토스트부터 다시 표시한다. |

제약:

- 활성 고객·업체 담당자·관리자 프로필만 앱 세션으로 인정한다.
- 인증 화면에 로그인 사용자가 진입하면 해당 역할 홈으로 돌려보낸다.
- 고객·업체 담당자·관리자가 다른 역할의 화면 그룹에 접근하면 자신의 역할 홈으로 돌려보낸다.
- 인증되지 않은 사용자가 보호 화면에 진입하면 로그인으로 보낸다.
- Android 하드웨어 뒤로가기 종료 보호는 고객의 최상위 탭 화면, 업체 담당자 홈, 관리자 운영 홈에서만 적용한다. 상세·입력 화면에서는 기존 내비게이션 스택의 뒤로가기를 유지한다.
- iOS와 웹에는 Android 하드웨어 뒤로가기 종료 보호를 적용하지 않는다.

### COM-002 로그인

| 항목 | 정의 |
|---|---|
| 상태 | `부분 구현` |
| 구현 파일 | `app/(auth)/login.tsx` |
| 대상 역할 | 고객·업체 담당자·관리자 |
| 목적 | 이메일과 비밀번호로 사용자 인증을 수행한다. |
| 입력 | 이메일, 비밀번호 |
| 데이터 | Supabase Auth 및 `profiles` |

| 액션 | 조건 | 결과 |
|---|---|---|
| 로그인 | 이메일·비밀번호 입력 완료 | 활성 고객은 `/(tabs)`, 활성 업체 담당자는 `/(partner)/dashboard`, 활성 관리자는 `/(admin)/dashboard`로 이동한다. |
| 고객 회원가입 | 로그인 처리 중이 아님 | `/(auth)/signup`으로 이동한다. |
| 임시 계정 배지 | 개발 모드이며 로그인 처리 중이 아님 | 이메일 라벨 오른쪽의 `고객`, `업체`, `관리자` 배지를 선택하면 해당 가데이터 계정의 이메일과 비밀번호를 입력란에 채운다. 로그인 요청은 별도로 로그인 버튼을 눌렀을 때만 실행한다. |

상태와 검증:

- 이메일 또는 비밀번호가 비어 있으면 입력 요청 오류를 표시한다.
- 이메일 형식이 잘못되면 올바른 주소 입력 오류를 표시한다.
- 로그인 요청 중에는 입력과 버튼을 비활성화하고 `로그인 중`을 표시한다.
- 인증 실패, 이메일 미확인, 비활성 계정, 네트워크 오류는 사용자용 문구로 표시한다.
- 고객은 직접 가입할 수 있고 업체 담당자와 관리자는 운영자 생성 계정으로 참여한다는
  안내를 표시한다.
- 임시 계정 배지는 `__DEV__`에서만 노출하고 선택한 계정 정보는 컴포넌트 입력 상태에만
  반영하며 별도로 저장하지 않는다.

미구현:

- 업체 담당자 앱 화면 진입
- 비밀번호 찾기

출시 전 제거:

- 임시 계정 배지와 가데이터 계정 정보는 스토어 배포 전에 로그인 화면에서 삭제한다.

### COM-003 접근 제한

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(tabs)/assignment.tsx` |
| 대상 역할 | 권한이 없는 사용자 |
| 목적 | 역할에 허용되지 않은 화면에 접근했음을 알린다. |
| 현재 상황 | 고객이 담당자 배정 경로에 접근한 경우 |

| 액션 | 결과 |
|---|---|
| 돌아가기 | Expo Router 이전 화면으로 이동한다. |

현재 이 화면은 탭에 노출되지 않는 숨김 경로다. 향후 공통 권한 오류 화면으로 재사용할 때
역할과 기능 이름을 매개변수화한다.

### COM-004 전역 오류 대체 화면

| 항목 | 정의 |
|---|---|
| 상태 | `부분 구현` |
| 구현 파일 | `src/shared/ui/ErrorBoundary.tsx`, `app/_layout.tsx` |
| 대상 역할 | 전체 |
| 목적 | 처리되지 않은 화면 렌더링 오류로 앱 전체가 빈 화면이 되는 것을 막는다. |

| 액션 | 결과 |
|---|---|
| Try Again | 오류 경계 상태를 초기화하고 화면 렌더링을 다시 시도한다. |

보완 필요:

- 현재 영문 안내를 한국어로 통일한다.
- 운영 환경에서는 민감 정보가 포함될 수 있는 원본 오류 메시지를 사용자에게 표시하지 않는다.

### COM-005 견적 상세·협업

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `src/widgets/remodel-request-detail/`, `app/(tabs)/request/[requestId].tsx`, `app/(partner)/requests/[requestId].tsx`, `app/(admin)/requests/[requestId].tsx` |
| 대상 역할 | 로그인 고객·업체 담당자·관리자 |
| 목적 | 같은 요청·선택 상세 화면을 공유하면서 고객, 업체 담당자와 관리자의 역할별 후속 액션을 제공한다. |
| 진입 | 고객 나의 견적 카드, 업체 배정 견적 카드 또는 관리자 견적 관리 카드 선택 |
| 데이터 | Supabase `remodel_requests`, `selection_snapshots`, `consultation_messages`, 역할별 제한 조회·상태 전환·견적 조정·고객 확인 RPC |
| 이탈 | 상단 목록 버튼으로 이전 화면 이동 |

공통 표시:

- 지역, 욕실 유형, 공사 범위, 예산, 희망 시기
- 추가 요청과 제품 선택 리포트, 첨부 사진 개수
- 선택 제품의 `base_price_snapshot` 합계
- 조정 견적 금액과 고객 확인 상태
- 로컬 견적 버전과 서버에 저장된 상담 타임라인

역할별 표시와 액션:

| 역할 | 표시·액션 | 조건 | 결과 |
|---|---|---|---|
| 고객        | 코멘트 보내기       | 공백이 아닌 메시지                                                                     | 요청 단위 수정 요청 메시지를 서버에 저장하고 고객·배정 업체·관리자 타임라인을 갱신한다.           |
| 고객 | 조정 견적 최종 확정 | `quote_adjustment`, 조정 금액 존재, 미확정 | 서버에 고객 확인 시각을 저장하고 확인 완료 상태를 표시한다. |
| 관리자 | 현재 견적 금액 | 항상 | 조정 전에는 선택 스냅샷 합계, 조정 후에는 최신 조정 금액을 크게 표시한다. |
| 관리자 | 수정 견적 금액 입력 | 요청이 `submitted` | 원 단위 정수를 입력한다. |
| 관리자 | 견적 조정 | 유효 금액 입력, 저장 중이 아님 | 조정 금액·관리자·조정 시각을 저장하고 요청 상태를 `quote_adjustment`로 변경한다. |
| 업체 담당자 | 현재 견적 금액 | 배정된 요청 열람 가능 | 조정 전에는 선택 스냅샷 합계, 조정 후에는 최신 조정 금액을 읽기 전용으로 표시한다. |
| 업체 담당자 | 견적 확인 및 진행 | 현재 업체 배정이 `assigned`, 요청이 `matched` 또는 `in_consultation` | 서버에서 배정을 `accepted`, 요청을 `in_consultation`으로 변경한다. |
| 업체 담당자 | 진행 불가 | 현재 업체 배정이 `assigned`, 요청이 `matched` 또는 `in_consultation`, 확인 메시지 승인 | 서버에서 현재 배정을 `declined`로 변경하고 남은 활성 배정이 없으면 요청을 `cancelled`로 변경한다. |
| 업체 담당자 | 시공 완료           | 현재 업체 배정이 `accepted`, 요청이 `in_consultation` 또는 `final_quote_sent`          | 서버에서 요청을 `closed`로 변경하고 업체 완료 견적 탭으로 이동시킨다.                             |
| 관리자      | 시공 완료           | 요청이 `in_consultation` 또는 `final_quote_sent`                                       | 서버에서 요청을 `closed`로 변경하고 관리자 완료·종료 탭으로 이동시킨다.                           |

규칙:

- 공통 UI와 역할 분기는 하나의 widget에서 관리하고 경로 파일은 역할과 요청 ID만 전달한다.
- 원 견적 금액은 서버에 저장된 선택 스냅샷 단가 합계이며 클라이언트 입력값을 신뢰하지 않는다.
- 관리자의 초기 견적 조정은 `submitted` 상태에서 한 번만 가능하다. `quote_adjustment`로
  전환된 뒤에는 고객 확인 전후와 관계없이 입력과 조정 버튼을 비활성화한다.
- 고객 확인은 `quote_adjustment` 상태를 유지한다. 이후 업체 배정이 완료될 때 `matched`로
  전환한다.
- 이 초기 조정 금액은 업체 배정 후 생성되는 `quotes` 견적 버전과 구분한다.
- 관리자 모드에는 코멘트 입력과 고객용 최종 컨펌 버튼을 표시하지 않는다.
- 업체 모드에는 관리자 전용 견적 금액 입력·조정과 고객용 확정·코멘트 입력을 표시하지 않는다.
- `시공 완료`는 클라이언트 직접 갱신이 아니라 권한과 현재 상태를 재검증하는 공통 RPC로만
  저장한다. 완료 후 동일 요청에 대한 중복 완료 요청은 서버에서 거부한다.
- 업체 배정 전 고객·관리자 코멘트는 `consultation_messages.assignment_id = null`인 요청 단위
  메시지로 저장한다. 요청 소유 고객과 관리자뿐 아니라 해당 요청에 배정되어 상세 열람 권한이
  있는 활성 업체 담당자도 배정 전후의 전체 요청 단위 상담 타임라인을 조회할 수 있다.
- 코멘트 등록만으로 요청 상태를 변경하지 않으며 현재 `submitted` 또는 `quote_adjustment`
  상태를 유지한다.

검증과 상태:

- 조정 금액은 0원 이상 1조 원 이하의 정수다.
- 요청 로딩, 조회 실패·재시도, 요청 없음, 상담 타임라인 로딩·실패, 코멘트 저장 중·실패,
  조정 저장 중·실패·완료, 고객 확인 중·실패·완료, 시공 완료 저장 중·실패·완료를 제공한다.
- 고객·관리자는 기존 역할 권한으로, 업체 담당자는 연결된 업체의 배정 범위에서만 RLS와
  제한 조회·응답 RPC 검증을 통과할 수 있다.

## 5. 고객 화면 정의

### CUS-001 고객 회원가입

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(auth)/signup.tsx` |
| 대상 역할 | 비로그인 고객 |
| 목적 | 고객 계정을 생성하고 이메일 확인 단계로 연결한다. |
| 입력 | 이름, 이메일, 비밀번호, 비밀번호 확인, 필수 약관 동의 |
| 데이터 | Supabase Auth 회원가입, 사용자 메타데이터의 `display_name` |

| 액션 | 조건 | 결과 |
|---|---|---|
| 로그인 | 가입 처리 중이 아님 | 이전 로그인 화면으로 이동한다. |
| 필수 동의 | 가입 처리 중이 아님 | 약관 동의 선택을 전환한다. |
| 회원가입 | 모든 검증 통과 | 계정을 생성하고 이메일 확인 안내로 이동한다. |

검증:

- 이름은 공백 제거 후 2자 이상이다.
- 이메일 형식이 유효해야 한다.
- 비밀번호는 영문 소문자와 특수기호를 포함해 8자 이상이다.
- 비밀번호와 비밀번호 확인이 일치해야 한다.
- 필수 약관에 동의해야 한다.

상태:

- 처리 중에는 전체 입력과 버튼을 비활성화하고 `가입 중`을 표시한다.
- 가입 실패 시 서버 오류를 안전한 사용자용 문구로 표시한다.
- 가입 성공 시 이메일을 검색 파라미터로 전달한다.

### CUS-002 이메일 확인 안내

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(auth)/verify-email.tsx` |
| 대상 역할 | 가입 직후 고객 |
| 목적 | 이메일 확인 절차를 안내하고 확인 메일을 재전송한다. |
| 입력 | 가입 화면에서 전달된 이메일 검색 파라미터 |
| 데이터 | Supabase Auth 확인 메일 재전송 |

표시 순서:

1. 이메일에서 바스페이스 확인 링크 열기
2. 이메일 확인 완료하기
3. 앱으로 돌아와 로그인하기

| 액션 | 조건 | 결과 |
|---|---|---|
| 로그인으로 이동 | 항상 | 기존 인증 기본 버튼과 동일한 48px 높이로 표시하고 `/(auth)/login`으로 교체 이동한다. |
| 확인 이메일 다시 보내기 | 이메일 파라미터 존재 | 확인 메일을 다시 요청한다. |

상태:

- 재전송 중에는 버튼을 비활성화하고 `보내는 중`을 표시한다.
- 성공하면 `확인 이메일을 다시 보냈습니다.`를 표시한다.
- 실패하면 사용자용 오류 문구를 표시한다.
- 이메일 파라미터가 없으면 일반 안내만 표시하고 재전송 버튼을 숨긴다.

### CUS-003 나의 견적

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(tabs)/index.tsx`, `app/(tabs)/_layout.tsx`, `src/features/view-remodel-requests/` |
| 대상 역할 | 로그인 고객 |
| 목적 | 고객 본인의 요청과 최신 진행 상태를 한눈에 보여준다. |
| 데이터 | Supabase `remodel_requests`, `selection_snapshots`, TanStack Query, 화면 협업용 `useRemodelRequestStore`, 로컬 `useQuoteStore` |

탭:

- 견적요청
- 나의견적
- 알림
- 설정

탭은 위 순서대로 표시한다.

표시 정보:

- 사용자 이름
- 고객 본인의 견적 요청 수
- 요청별 지역, 욕실 유형, 공사 범위, 예산
- 요청 상태 배지
- 최신 견적 버전 존재 여부

| 액션 | 조건 | 결과 |
|---|---|---|
| 견적 요청 | 항상 | `/(tabs)/explore`로 이동한다. |
| 요청 카드 | 해당 요청 존재 | `/(tabs)/request/[requestId]`로 이동한다. |
| 당겨서 새로고침 | 요청 목록 표시 중 | 고객 요청 Query를 다시 조회해 최신 상태와 건수를 반영한다. |

상태:

- 요청 로딩: 서버 조회가 끝날 때까지 로딩 안내를 표시한다.
- 요청 조회 실패: 네트워크 안내와 다시 시도 액션을 표시한다.
- 요청 없음: `아직 요청이 없어요.`와 견적 요청 안내를 표시한다.
- 요청 있음: RLS와 고객 ID 조건으로 로그인 고객 본인의 요청만 최신순으로 표시한다.
- 화면에 다시 진입하면 고객 요청 Query를 다시 조회해 관리자 조정 등 다른 역할에서 변경된
  최신 상태를 반영한다.
- 제출 성공 시 고객 요청 Query를 무효화해 서버 저장 결과를 다시 조회한다.

### CUS-004 견적 요청

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(tabs)/explore.tsx`, `src/features/create-remodel-request/`, `src/features/select-quote-options/` |
| 대상 역할 | 로그인 고객 |
| 목적 | 공사 주소, 예산, 욕실 사진, 관리자 등록 제품과 추가 요청을 입력하고 관리자 검토 대기 상태로 견적을 접수한다. |
| 데이터 | Supabase `quote_option_masters`, `quote_option_products`, `remodel_requests`, `selection_snapshots`; 사진 URI와 작성 중 입력은 컴포넌트 로컬 상태 |

입력과 선택:

| 구분 | 항목 | 동작 |
|---|---|---|
| 기본 조건 | 기본 주소 | 수정 불가 입력과 `주소 입력` 버튼을 한 행에 표시한다. 버튼을 누르면 Kakao 우편번호 검색 다이얼로그가 열리고, 주소 선택 후 `확인`을 눌러 반영한다. |
| 기본 조건 | 상세 주소 | 고객이 동·호수 등 나머지 주소를 직접 입력한다. |
| 기본 조건 | 희망 예산 | `150~200만원`, `200~300만원`, `300~500만원`, `견적 협의` 중 하나를 선택한다. 화면에는 한글 라벨을 표시하고 DB `budget_range`에는 각각 `KRW_150_200`, `KRW_200_300`, `KRW_300_500`, `CONSULTATION` 코드를 저장한다. |
| 고정 저장값 | 주거·욕실·크기 등 | 아파트, 공용 욕실, 약 3㎡, 철거 필요, 전체 리모델링, 2개월 이내 |
| 사진 | 욕실 사진 | 기기 사진 라이브러리에서 최대 5장 선택하고 개별 삭제한다. 선택 사진은 로컬 미리보기이며 이번 범위에서는 서버에 업로드하지 않는다. |
| 중요 선택 옵션 | 관리자 옵션 9개 | 활성 옵션을 `display_order`, 이름 순으로 로드한다. 각 행 왼쪽에 빈 체크박스를 표시한다. |
| 중요 선택 옵션 | 제품 선택 | 체크박스를 선택하면 제품 이미지·이름·단가 갤러리가 아래로 애니메이션되어 열린다. 체크한 옵션은 제품 하나가 필수이며 같은 옵션에서 중복 선택할 수 없다. 체크하지 않은 옵션은 필수가 아니다. 제품이 없는 옵션은 체크할 수 없다. |
| 예상 선택 금액 | 합계 | 선택된 각 제품의 관리자 등록 단가를 합산해 화면 하단에 원 단위로 표시한다. 제출 시 서버가 제품의 현재 단가를 다시 조회해 스냅샷으로 저장한다. |
| 추가 요청 | 요청 사항 | 여러 줄 자유 입력 |

| 액션 | 조건 | 결과 |
|---|---|---|
| 주소 입력 | 항상 | Kakao 우편번호 검색 다이얼로그를 연다. |
| 주소 검색 확인 | 검색 결과 주소 선택 | 기본 주소에 선택값을 반영하고 다이얼로그를 닫는다. |
| 예산 칩 선택 | 항상 | 선택 예산을 변경한다. |
| 사진 선택하기 | 5장 미만 | 사진 권한 요청 후 라이브러리를 연다. |
| 사진 삭제 | 선택 사진 존재 | 해당 사진을 화면 상태에서 제거한다. |
| 옵션 체크 | 해당 옵션에 제품 존재 | 제품 갤러리를 열고 아직 선택한 제품이 없으면 옵션 검증 대상으로 표시한다. |
| 옵션 체크 해제 | 해당 옵션이 열림 | 제품 선택을 제거하고 합계에서 해당 단가를 뺀다. |
| 제품 선택 | 옵션 체크됨 | 같은 옵션의 기존 선택을 교체하고 합계를 다시 계산한다. |
| 옵션 다시 불러오기 | 옵션 조회 실패 | 관리자 옵션과 제품을 다시 조회한다. |
| 견적 요청 보내기 | 로그인 고객이고 제출 중이 아님 | 주소·예산·체크한 옵션의 제품 선택을 검증하고, 통과하면 요청과 제품·단가 스냅샷을 서버에 저장한 뒤 상태를 `submitted`로 전환한다. |
| 완료 모달 확인 | 제출 완료 | 입력 상태를 정리하고 `/(tabs)`로 이동한다. |

검증과 상태:

- 기본 주소가 비어 있으면 기본 조건 섹션에 빨간 오류 라벨을 표시한다.
- 희망 예산을 선택하지 않으면 기본 조건 섹션에 빨간 오류 라벨을 표시한다.
- 체크한 옵션에 제품이 선택되지 않으면 중요 선택 옵션 섹션과 해당 옵션에 빨간 오류 라벨을 표시한다.
- 옵션 로딩 중에는 로딩 안내를 표시하고 제출을 비활성화한다.
- 옵션 조회가 실패하면 오류 안내와 `다시 시도` 버튼을 표시한다.
- 제출 중에는 입력과 제출 버튼의 중복 동작을 막는다.
- 서버 제출 실패 시 입력을 유지하고 제출 섹션에 재시도 가능한 오류를 표시한다.
- 제출 성공 시 `submitted` 상태로 저장하고 관리자의 확인을 기다린다는 완료 모달을 표시한다.
- 사진이 이미 5장이면 최대 등록 수 안내를 표시한다.
- 사진 권한이 거부되면 권한 필요 안내를 표시한다.
- 로그인 사용자가 없으면 재로그인 안내 후 로그인 화면으로 이동한다.
- 사진은 선택 사항이며 0장으로 제출할 수 있다.

현재 제약:

- 요청 본문과 제품 선택 스냅샷은 Supabase에 저장하지만 사진 URI는 같은 세션의 메모리에만 유지되며 로그아웃·앱 재시작 후 사라진다.
- 파일 크기와 JPG·PNG·HEIC 형식 검증은 화면에 연결되지 않았다.
- 상세 주소는 선택 사항이다.
- 고객은 옵션을 체크하지 않고 제출할 수 있다.
- 완료 모달은 뒤로가기 요청으로 닫히지 않고 `확인`을 눌러야 한다.

### CUS-005 고객 견적 상세 진입

| 항목 | 정의 |
|---|---|
| 상태 | `부분 구현` |
| 구현 파일 | `app/(tabs)/request/[requestId].tsx`, `src/widgets/remodel-request-detail/`, `src/features/view-remodel-request-detail/` |
| 대상 역할 | 로그인 고객 |
| 목적 | 공통 COM-005를 고객 모드로 열어 요청 리포트, 조정 견적, 견적 버전과 상담 메시지를 확인한다. |
| 데이터 | Supabase 요청·선택·조정 견적·`consultation_messages`와 TanStack Query, 로컬 견적 Zustand 스토어 |

표시 정보:

- 지역, 욕실 유형, 공사 범위, 예산, 희망 시기
- 추가 요청과 제품 선택 리포트
- 첨부 사진 개수
- 견적 버전별 합계, 항목명, 유효기간, 부가세 포함 여부
- 상담 참여자와 메시지
- 관리자가 입력한 조정 견적 금액과 확인 여부

| 액션 | 조건 | 결과 |
|---|---|---|
| 목록 | 항상 | 이전 화면으로 이동한다. |
| 코멘트 보내기 | 공백이 아닌 메시지 | 수정 요청 유형 메시지를 서버에 저장하고 상담 타임라인을 갱신한다. |
| 조정 견적 최종 확정 | 조정 견적이 있고 아직 확인하지 않음 | 서버에 고객 확인 시각을 저장한다. |
| 이 견적 최종 컨펌 | 미확정 견적 존재 | 최신 견적을 확정하고 요청을 `confirmed`로 변경하며 확인 메시지를 추가한다. |
| 목록으로 돌아가기 | 요청을 찾지 못함 | 이전 화면으로 이동한다. |

상태:

- 요청 로딩: 고객 요청 Query가 완료될 때까지 로딩 안내를 표시한다.
- 요청 조회 실패: 네트워크 안내와 다시 시도 액션을 표시한다.
- 요청 없음: 찾을 수 없음 안내와 목록 복귀 버튼을 표시한다.
- 견적 없음: 업체가 리포트를 검토 중이라는 빈 상태를 표시한다.
- 메시지 없음: 별도 빈 상태 문구 없이 입력 영역만 표시한다.
- 코멘트 저장 중에는 입력과 전송 버튼을 비활성화하고, 실패하면 입력 내용을 유지한 채
  재시도 안내를 표시한다.

현재 제약:

- 앱 내부에 견적 생성 화면과 초기 견적 데이터가 없어 정상 사용자 흐름으로는 견적 카드와
  최종 컨펌 버튼에 도달할 수 없다.
- 화면에 여러 견적 카드가 있어도 모든 컨펌 버튼은 정렬상 최신 견적을 확정한다.
- 최종 견적 상태인지 확인하지 않고 `confirmed`가 아닌 모든 견적에 컨펌 버튼을 표시한다.
- 고객 요청은 RLS와 고객 ID 조건으로 조회하며, Query 결과에 없는 요청 ID는 찾을 수 없음으로 처리한다.
- 조정 견적 금액과 고객 확인은 서버에 저장하고, 업체 견적 버전·메시지·기존 최종 컨펌은
  아직 로컬 상태다.
- 코멘트는 모두 `수정 요청` 유형으로 저장되며 질문·확인 선택 UI는 없다.
- 사진은 개수만 표시하고 이미지 미리보기는 제공하지 않는다.
- 최종 컨펌 완료 안내는 네이티브 `Alert`를 사용한다.

### CUS-007 설정

| 항목 | 정의 |
|---|---|
| 상태 | `부분 구현` |
| 구현 파일 | `app/(tabs)/settings.tsx` |
| 대상 역할 | 로그인 고객 |
| 목적 | 계정과 앱 관련 정보를 확인하고 보조 작업을 수행한다. |

| 항목·액션 | 결과 |
|---|---|
| 로그인 정보       | 현재 사용자 이름과 이메일을 요약해 표시하고 `CUS-009 로그인 정보`로 이동한다.                                                                |
| 공지사항 | 등록된 공지사항이 없다는 안내를 표시한다. |
| 로그아웃 | 확인창에서 취소 또는 로그아웃을 선택한다. 인증·SecureStore·고객 요청/견적/메시지 상태와 TanStack Query 캐시를 정리한 뒤 로그인으로 이동한다. |
| 앱스토어에서 보기 | iOS는 앱스토어 검색, Android는 앱 상세 주소를 연다. |

예외 상태:

- 로그아웃 실패 시 네트워크 확인 안내를 표시한다.
- 외부 스토어를 열지 못하면 재시도 안내를 표시한다.
- 로그아웃 처리 중 상태를 설정 행에 표시하지 않으며 중복 실행을 막지 않는다.

미구현:

- 실제 공지사항 목록
- 개인정보 처리방침·이용약관·문의·회원 탈퇴
- 확정된 iOS 앱 ID 기반 상세 주소

### CUS-009 로그인 정보

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(tabs)/account.tsx`                                 |
| 대상 역할 | 로그인 고객 |
| 목적      | 현재 로그인 계정 정보를 확인하고 이름·연락처를 수정한다. |
| 데이터    | Supabase Auth 이메일, `public.profiles` 본인 행          |

진입과 이탈:

- `CUS-007 설정`의 `로그인 정보` 행을 누르면 진입한다.
- 상단 `설정` 뒤로가기 버튼 또는 저장 완료 후 현재 화면에 머무르며 설정으로 돌아갈 수 있다.
- 고객 탭 바에는 별도 탭으로 노출하지 않는다.

표시·입력 정보:

- 이메일: 현재 Supabase Auth 이메일을 읽기 전용으로 표시한다.
- 이름: 현재 `profiles.display_name`을 표시하며 2자 이상 80자 이하로 수정할 수 있다.
- 연락처: 현재 `profiles.phone`을 표시한다. 값이 있으면 숫자 10~11자리만 허용하고,
  하이픈은 입력·저장 시 제거한다. 비워서 저장하면 `null`로 저장한다.

버튼·액션:

- `변경사항 저장`은 유효한 이름과 연락처가 있고, 최초 조회값과 달라졌을 때만 활성화한다.
- 저장 시 현재 인증 고객의 `display_name`, `phone`만 Supabase에 갱신한다.
- 저장 성공 시 TanStack Query 캐시와 인증 메모리 상태를 갱신해 화면과 설정 요약에 최신 이름을
  즉시 반영하고 완료 안내를 표시한다.

상태:

- 조회 중에는 로딩 안내를 표시한다.
- 조회 실패 시 네트워크 안내와 `다시 시도` 버튼을 표시한다.
- 저장 중에는 입력, 현재 화면 뒤로가기, 저장 버튼의 중복 실행을 막고 진행 상태를 표시한다.
  다른 고객 탭으로 이동해도 진행 중인 저장 요청과 완료 후 캐시 갱신은 유지한다.
- 검증 실패는 해당 입력 아래에 안내하며, 저장 실패 시 입력값을 유지하고 재시도 안내를
  표시한다.
- 프로필은 RLS로 본인 행만 조회·수정하며 이메일·역할·상태는 이 화면에서 수정하지 않는다.
- 서버도 이름 2~80자와 새로 입력하는 연락처의 `null` 또는 숫자 10~11자리 규칙을
  강제한다. 검증 추가 전에 저장된 다른 형식의 연락처는 이름만 수정할 때 그대로 보존하고,
  연락처를 직접 수정할 때 현재 형식으로 저장한다.

### CUS-008 알림

| 항목 | 정의 |
|---|---|
| 상태 | `부분 구현` |
| 구현 파일 | `app/(tabs)/notifications.tsx` |
| 대상 역할 | 로그인 고객                                        |
| 목적 | 고객에게 전달되는 알림을 확인할 진입점을 제공한다. |
| 데이터 | 없음 |

표시 정보:

- 화면 제목 `알림`
- 새 알림이 없다는 빈 상태 안내

상태:

- 현재는 정적 빈 상태만 표시한다.

미구현:

- 알림 데이터 모델과 Supabase 조회
- 알림 목록, 읽음 상태, 유형별 이동
- 푸시 알림 수신과 배지

## 6. 업체 담당자 화면 정의

업체 담당자는 관리자가 업체를 등록할 때 함께 생성한 계정으로 로그인한다. 연결된 업체에
배정된 견적을 조회하고, 상세 확인 후 진행 또는 진행 불가를 서버에 저장한다.

### PAR-001 배정 견적서 확인

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(partner)/requests/index.tsx`, `src/features/partner-request-management/` |
| 경로 | `/(partner)/requests` |
| 대상 역할 | 업체 담당자 |
| 목적 | 연결된 업체에 배정된 견적만 상태별로 확인한다. |
| 데이터 | 제한된 Supabase 조회 RPC가 반환하는 현재 업체의 `request_assignments`, `remodel_requests`, 고객 표시명 |
| 주요 표시 | 화면 제목, 4개 상태 탭과 건수, 고객명, 접수일, 지역·상세 주소, 예산, 희망 일정, 상태 |
| 주요 액션 | 상태 탭 선택, 견적 상세 이동, 당겨서 새로고침, 조회 실패 재시도 |
| 필수 상태 | 로딩, 탭별 배정 견적 없음, 조회 오류, 재시도, 새로고침 |

상태 탭:

| 탭 | 포함 조건 |
|---|---|
| `배정 견적` | 현재 업체의 배정 상태가 `assigned`인 견적 |
| `시공중 견적` | 배정 상태가 `accepted`이고 요청 상태가 `in_consultation` 또는 `final_quote_sent`인 견적 |
| `완료 견적` | 배정 상태가 `accepted`이고 요청 상태가 `confirmed` 또는 `closed`인 견적 |
| `취소 견적` | 현재 업체가 `declined`했거나 요청 상태가 `cancelled`인 견적 |

규칙:

- 목록 데이터는 현재 로그인 담당자가 활성 구성원으로 연결된 업체에 배정된 항목만 반환한다.
- 동일 요청이 여러 업체에 배정된 경우 각 업체에는 자신의 배정 응답 상태만 표시한다.
- 클라이언트 탭 라벨인 `시공중 견적`은 별도 DB 상태를 만들지 않고 관리자 `견적 진행`
  탭과 같은 `in_consultation`, `final_quote_sent` 상태를 사용한다.
- 목록 카드는 버튼이며 선택하면 같은 배정의 `PAR-002`로 이동한다.

### PAR-002 견적 상세

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(partner)/requests/[requestId].tsx`, `src/widgets/remodel-request-detail/`, `src/features/partner-request-management/` |
| 경로 | `/(partner)/requests/[requestId]` |
| 대상 역할 | 업체 담당자 |
| 목적 | 배정된 고객 견적을 검토하고 진행 가능 여부를 확정한다. |
| 데이터    | Supabase 요청·선택 스냅샷·허용된 고객 표시명과 현재 업체 배정 정보, 요청 단위 전체 상담 타임라인, 상태 전환 RPC             |
| 주요 표시 | 고객 조건, 제품 선택, 조정된 현재 견적 금액, 배정 정보, 견적 버전, 상담 타임라인 |
| 주요 액션 | 목록 복귀, `견적 확인 및 진행`, `진행 불가`, `시공 완료`                                                                    |
| 필수 상태 | 권한 없음, 로딩, 조회 오류, 배정 대기, 진행 중, 완료, 취소, 저장 중·실패 |

규칙:

- 업체 담당자는 연결된 업체에 배정된 요청만 열람한다.
- 상세 열람 권한이 있는 업체 담당자는 배정 전에 고객·관리자가 남긴 메시지를 포함해 해당
  요청의 전체 상담 타임라인을 고객·관리자와 동일하게 조회한다.
- `견적 확인 및 진행`은 현재 배정이 `assigned`이고 요청이 `matched` 또는
  `in_consultation`일 때만 표시하며, 서버 RPC가 배정을 `accepted`로, 요청을
  `in_consultation`으로 원자적으로 변경한다.
- `진행 불가` 선택 시 `견적을 진행하지 않으시겠습니까?` 확인 메시지를 표시한다.
- 진행 불가 확인 시 현재 업체 배정을 `declined`로 변경한다. 다른 활성 배정이 없을 때만
  요청 전체를 `cancelled`로 변경하며, 다른 업체의 활성 배정은 취소하지 않는다.
- 요청이 `confirmed`, `closed`, `cancelled`인 종료 상태에서는 배정 응답 버튼을 표시하지
  않고 서버도 진행·진행 불가 응답을 모두 거부한다.
- 응답 성공 즉시 목록과 상세 캐시에 새 배정 상태를 반영해 버튼 로딩을 종료하고, 전체 견적
  서버 캐시는 한 번의 백그라운드 재검증으로 갱신한다.
- `시공 완료`는 현재 배정이 `accepted`이고 요청 상태가 `in_consultation` 또는
  `final_quote_sent`일 때만 표시한다. 성공하면 요청을 `closed`로 변경하고 업체 목록의
  `완료 견적` 탭에 포함한다.
- 업체 화면에서는 관리자 전용 수정 견적 금액 입력과 `견적 조정` 액션을 표시하지 않는다.
- 고객 개인정보는 상담에 필요한 범위만 노출한다.

### PAR-003 견적 작성·수정

| 항목 | 정의 |
|---|---|
| 상태 | `계획` |
| 대상 역할 | 업체 담당자 |
| 목적 | 고객 요청을 바탕으로 버전이 보존되는 견적을 작성한다. |
| 입력 | 항목, 수량, 단가, 할인, 부가세 포함 여부, 비고, 유효기간 |
| 주요 액션 | 항목 추가·삭제, 임시 저장, 일반 견적 발송, 최종 견적 발송 |
| 필수 상태 | 작성 중, 검증 오류, 저장 중, 발송 확인, 발송 완료, 발송 실패 |

규칙:

- 발송한 견적은 수정하지 않고 새 버전으로 작성한다.
- 이전 버전은 읽기 전용으로 보존한다.
- 최종 견적 여부를 발송 전에 명확히 확인한다.

### PAR-004 업체 담당자 홈

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(partner)/_layout.tsx`, `app/(partner)/dashboard.tsx`, `src/features/partner-management/` |
| 경로 | `/(partner)/dashboard` |
| 대상 역할 | 업체 담당자 |
| 목적 | 업체 담당자 인증과 업체 연결을 확인하고 배정 견적 업무로 진입한다. |
| 데이터 | 인증 세션의 이름·이메일·역할, 활성 `partner_members`와 연결된 업체명 |
| 주요 표시 | 업체 담당자 배지, 업체명, 담당자 이름·로그인 이메일, `배정 견적서 확인` 메뉴 |

| 액션 | 조건 | 결과 |
|---|---|---|
| 배정 견적서 확인 | 활성 업체 연결 조회 완료 | `/(partner)/requests`로 이동한다. |
| 로그아웃 | 로그아웃 처리 중이 아님 | Supabase 로그아웃과 보안 저장소 정리 후 로그인으로 이동한다. |
| 다시 시도 | 업체 연결 조회 실패 | 현재 로그인 사용자의 업체 연결을 다시 조회한다. |

상태:

- 로딩: 연결 업체 정보를 불러오는 중이라는 문구를 표시한다.
- 연결 완료: 업체명과 로그인 계정 정보를 표시한다.
- 연결 없음: 운영자에게 업체 연결을 요청하라는 안내를 표시한다.
- 조회 오류: 네트워크 확인 안내와 다시 시도를 표시한다.
- 업체 연결이 없거나 조회 중이면 `배정 견적서 확인` 메뉴를 비활성화한다.

## 7. 관리자 화면 정의

관리자는 실제 Supabase 인증 후 관리자 운영 홈에 진입할 수 있다. 견적 옵션 관리와 견적
관리 목록·상세는 실제 Supabase와 연결되어 있다.
과거 로컬 배정 프로토타입은 현재 제거되었으며 `assignment` 경로에는 고객 접근 제한
화면만 남아 있다.

### ADM-001 관리자 운영 홈

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(admin)/_layout.tsx`, `app/(admin)/dashboard.tsx` |
| 대상 역할 | 관리자 |
| 목적 | 견적·시공과 운영 기능을 두 섹션으로 구분해 관리 기능과 준비 중인 범위를 안내한다. |
| 데이터 | 인증 세션의 관리자 이름·이메일·역할 |
| 주요 표시 | 관리자 역할 배지, 이름, 이메일, `견적 및 시공 관리`·`운영 관리` 섹션과 3개 메뉴 카드 |

메뉴 구성:

- `견적 및 시공 관리`: `견적 관리`, `업체 관리`
- `운영 관리`: `견적 옵션 관리`
- 기존 `배정 관리` 메뉴는 제거하며, 요청 상세에서의 업체·담당자 배정 계획은 `ADM-003`에 유지한다.

| 액션 | 조건 | 결과 |
|---|---|---|
| 견적 관리 | 항상 | `/(admin)/requests`로 이동한다. |
| 업체 관리 | 항상 | `/(admin)/partners`로 이동한다. |
| 견적 옵션 관리 | 항상 | `/(admin)/catalog`로 이동한다. |
| 로그아웃 | 로그아웃 처리 중이 아님 | Supabase 로그아웃과 보안 저장소 정리 후 로그인 화면으로 이동한다. |

상태:

- 로그아웃 처리 중에는 버튼을 비활성화하고 진행 상태를 표시한다.
- 로그아웃 실패 시 네트워크 확인 안내를 표시한다.
- 이동 가능한 견적 관리, 업체 관리와 견적 옵션 관리 카드에는 상태 배지를 표시하지 않는다.

미구현:

- 전체 요청·매칭·미응답 현황 수치
- 요청 상세에서 업체·담당자 배정

### ADM-002 견적 관리

| 항목 | 정의 |
|---|---|
| 상태 | `부분 구현` |
| 구현 파일 | `app/(admin)/requests/index.tsx`, `src/features/admin-request-management/` |
| 경로 | `/(admin)/requests` |
| 대상 역할 | 관리자 |
| 목적 | 모든 고객이 제출한 요청을 최신순으로 조회하고 DB 상태에 맞는 업무 단계별로 분류한다. |
| 데이터 | Supabase `remodel_requests`, `profiles`, `request_assignments`, `partners` 관리자 조회 결과 |
| 필터 | 상단 5개 상태 탭 |
| 주요 표시 | 화면 설명, 탭별 요청 수, 원본 상태 배지, 고객명, 지역·상세 주소, 예산, 희망 일정, 접수일, 배정 업체 |
| 주요 액션 | 상태 탭 선택, 요청 상세 이동, 당겨서 새로고침, 조회 실패 재시도 |
| 필수 상태 | 로딩, 탭별 검색 결과 없음, 조회 오류, 재시도, 새로고침 |

상태 탭:

| 탭 | 포함하는 `remodel_request_status` | 표시 규칙 |
|---|---|---|
| `신규 견적` | `submitted` | 관리자 배정을 기다리는 제출 요청 |
| `견적 조율` | `quote_adjustment` | 조정 금액과 고객 확인 여부를 카드 배지로 구분 |
| `배정 완료` | `matched` | 업체 또는 담당자가 배정된 요청 |
| `견적 진행` | `in_consultation`, `final_quote_sent` | 원본 상태를 `견적 협의 중`, `최종 견적 도착` 배지로 구분 |
| `완료·종료` | `confirmed`, `closed`, `cancelled`    | 원본 상태를 `최종 컨펌`, `시공 완료`, `취소` 배지로 구분 |

탭 레이아웃:

- 탭 영역은 기기 가로 너비 전체를 사용하며 5개 버튼을 동일한 너비로 배치한다.
- 각 버튼에는 탭 이름과 해당 요청 건수를 두 줄로 표시하고 최소 64px 높이를 확보한다.
- 선택 탭은 세이지 배경, 딥 틸 텍스트와 하단 강조선으로 구분한다.
- 탭 바로 아래에는 선택된 목록 이름과 건수를 다시 표시해 탭과 요청 카드 목록이 하나의
  연속된 영역으로 인식되도록 한다.

규칙:

- 탭 분류는 클라이언트 전용 상태를 만들지 않고 DB의 `remodel_request_status` 값을 그대로
  사용한다.
- `draft`는 고객이 제출하지 않은 작성 중 상태이므로 관리자 견적 관리 목록과 탭 건수에서
  제외한다.
- `confirmed`는 계약 또는 시공 완료가 아니며 `closed`와도 의미가 다르므로, 같은 탭에서도
  각각의 원본 상태 배지를 유지한다.
- `cancelled`는 데이터를 삭제하지 않고 취소 이력을 보존하는 종료 상태다.
- 요청 카드는 버튼이며 선택하면 같은 요청 ID의 `ADM-003`으로 이동한다.
- 상태 enum, DB 스키마와 상태 전이 규칙은 별도 사용자 승인 없이 변경하지 않는다.

현재 제약:

- 지역·매칭 업체·생성일의 추가 필터는 계획 상태다.
- 이 화면은 상태를 조회만 하며 상태 변경 기능을 제공하지 않는다.

### ADM-003 관리자 견적 상세·조정

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(admin)/requests/[requestId].tsx`, `src/widgets/remodel-request-detail/` |
| 경로 | `/(admin)/requests/[requestId]` |
| 대상 역할 | 관리자 |
| 목적 | 공통 COM-005를 관리자 모드로 열어 고객 요청을 확인하고 초기 견적 금액 조정과 업체 배정을 수행한다. |
| 데이터    | Supabase 요청·선택 스냅샷·고객 프로필·조정 금액·상담 메시지·승인 업체·대표 담당자와 관리자 조정·배정·시공 완료 RPC |
| 주요 액션 | 목록 복귀, 고객 코멘트 확인, 수정 견적 금액 입력, 견적 조정, 업체 배정 모달 열기, 업체 선택·확인·취소, `시공 완료` |
| 필수 상태 | 요청·상담·업체 목록 로딩·오류·없음, 금액 검증, 조정·배정·시공 완료 저장 중·실패·완료                               |

규칙:

- 요청 상세·제품 선택과 원 견적 합계는 읽기 전용이다.
- 고객이 저장한 요청 단위 코멘트를 상담 타임라인에 생성순으로 표시한다.
- `submitted` 상태에서만 견적 조정을 실행할 수 있다. `quote_adjustment` 이후에는 수정
  견적 입력과 버튼을 표시하되 비활성화하고, 고객 확인 여부에 맞는 안내를 표시한다.
- 견적 조정 성공 시 목록과 상세 Query를 갱신하고 요청은 `견적 조율` 탭으로 이동한다.
- `업체 배정` 버튼은 견적 조정 섹션 아래에 별도 배치하며 `submitted`,
  `quote_adjustment` 상태에서만 활성화한다.
- 버튼을 누르면 `등록업체 리스트` 모달을 열고 승인된 업체를 업체명과
  `대표담당자이름(이메일)` 두 줄 버튼으로 표시한다.
- 업체 선택 시 `"[업체명] 업체에 견적을 배정하시겠습니까?"` 확인 화면을 같은 모달에서
  표시한다. `취소`는 모달을 닫고, `확인`은 배정 저장을 실행한다.
- 배정 RPC는 관리자 권한, 요청 상태, 승인 업체와 활성 대표 담당자를 재검증한 뒤
  `request_assignments` 생성과 요청 상태의 `matched` 전환을 한 트랜잭션으로 처리한다.
- 배정 RPC 성공 시 모달을 즉시 닫고 `/(admin)/requests`로 교체 이동한다. 관련 Query
  갱신은 화면 이동을 막지 않는 백그라운드 작업으로 실행한다.
- `시공 완료`는 요청이 `in_consultation` 또는 `final_quote_sent`일 때만 표시한다. 공통
  완료 RPC가 관리자 권한과 현재 상태를 재검증한 뒤 요청을 `closed`로 변경하며, 성공하면
  관리자 목록의 `완료·종료` 탭에 포함한다.
- 업체 목록이 비었거나 조회·배정에 실패하면 모달 안에서 안내와 재시도를 제공한다.

### ADM-004 업체 목록

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(admin)/partners/index.tsx` |
| 경로 | `/(admin)/partners` |
| 대상 역할 | 관리자 |
| 목적 | 등록된 전체 업체를 한눈에 확인하고 신규 등록 또는 상세 열람으로 이동한다. |
| 데이터 | Supabase `partners` 최신 등록순 목록과 전체 개수 |
| 주요 표시 | 좌측 상단 `총 N업체` 라벨, 우측 상단 `업체 추가` 버튼, 업체명·사업자등록번호·대표 로그인 이메일·담당자 이름·담당자 연락처 목록 |

| 액션 | 조건 | 결과 |
|---|---|---|
| 업체 추가 | 항상 | `/(admin)/partners/new`로 이동한다. |
| 업체 선택 | 목록 행이 존재 | 선택한 `/(admin)/partners/[partnerId]`로 이동한다. |
| 다시 시도 | 목록 조회 실패 | 업체 목록을 다시 조회한다. |

상태:

- 로딩: 중앙 로딩 표시와 업체 목록을 불러오는 중이라는 문구를 표시한다.
- 빈 상태: `등록된 업체가 없어요.` 문구와 첫 업체 등록 안내를 표시한다.
- 데이터 있음: 최신 등록 업체부터 카드 목록으로 표시한다.
- 조회 오류: 네트워크 확인 안내와 `다시 시도` 버튼을 표시한다.
- 등록 화면에서 저장 후 돌아오면 캐시를 갱신해 새 업체를 즉시 목록에 표시한다.

현재 제약:

- 업체 수정·삭제·비활성화와 담당자 계정 초대는 아직 제공하지 않는다.
- 관리자가 직접 등록한 업체는 `approved` 상태로 저장한다.

### ADM-006 업체 추가

| 항목 | 정의 |
|---|---|
| 상태 | `부분 구현` |
| 구현 파일 | `app/(admin)/partners/new.tsx`, `src/features/partner-management/` |
| 경로 | `/(admin)/partners/new` |
| 대상 역할 | 관리자 |
| 목적 | 업체 정보와 대표 담당자의 로그인 계정·연락 정보를 함께 등록한다. |
| 데이터 | 관리자 전용 Edge Function, Supabase Auth, `profiles`, `partners`, `partner_members`, `partner_login_accounts`, 비공개 Storage `partner-documents` |
| 입력 | 업체명(필수), 사업자등록번호(필수), 업체 사업자등록증 이미지 1장(선택), 업체 대표 이메일(필수·로그인용), 패스워드(필수), 담당자 이름(필수), 담당자 연락처(필수), 비고(선택) |

| 액션 | 조건 | 결과 |
|---|---|---|
| 뒤로 | 등록 처리 중이 아님 | 업체 목록으로 돌아간다. |
| 이미지 첨부 | 등록 처리 중이 아님 | 이미지 라이브러리 권한을 요청하고 이미지 1장을 선택·미리보기한다. |
| 이미지 변경 | 선택 이미지 존재, 등록 처리 중이 아님 | 이미지 1장을 다시 선택한다. |
| 이미지 삭제 | 선택 이미지 존재, 등록 처리 중이 아님 | 선택한 로컬 이미지를 제거한다. |
| 업체 등록 | 필수 입력 6개가 유효하며 등록 처리 중이 아님 | 사업자등록번호·로그인 이메일 중복을 확인하고 업체, Auth 사용자, 대표 멤버 연결과 선택 이미지를 저장한 뒤 업체 목록으로 돌아간다. |

검증과 업무 규칙:

- 입력 항목은 총 8개이며 업체명·사업자등록번호·업체 대표 이메일·패스워드·담당자 이름·담당자 연락처 6개가 필수다.
- 필수 입력이 하나라도 비어 있으면 하단 `업체 등록` 버튼을 비활성화한다.
- 업체 대표 이메일은 이메일 형식을 확인하고 소문자로 정규화한다.
- 패스워드는 고객 회원가입과 동일하게 8자 이상이며 영문 소문자와 특수문자를 각각 하나
  이상 포함해야 한다.
- 비밀번호는 Edge Function에 HTTPS 요청으로 전달해 Auth 사용자 생성에만 사용하며
  폼 입력 중 메모리에만 유지한다. 영구 앱 상태·DB·문서·로그에 저장하거나 응답으로
  반환하지 않는다.
- 사업자등록번호는 하이픈을 제외한 숫자 기준으로 중복만 검사하며 별도 번호 체계 유효성 검사는 하지 않는다.
- 중복이면 `이미 등록된 사업자등록번호예요.`를 표시하고 저장하지 않는다.
- 이미 사용 중인 로그인 이메일이면 `이미 등록된 로그인 이메일이에요.`를 표시하고 저장하지 않는다.
- DB에도 정규화된 사업자등록번호 고유 제약을 적용해 동시 등록 중복을 차단한다.
- 사업자등록증은 JPEG·PNG·HEIC·HEIF 이미지 1장, 최대 10MB로 제한하고 관리자만 접근 가능한 비공개 버킷에 저장한다.
- 생성된 Auth 사용자는 이메일 확인 완료, `partner_staff / active` 프로필로 설정하고 해당
  업체의 활성 대표 멤버로 연결해 즉시 로그인할 수 있게 한다.
- 등록 중에는 모든 입력·첨부·이동·등록 액션을 비활성화해 중복 실행을 막는다.

상태:

- 초기: 빈 폼과 비활성 `업체 등록` 버튼을 표시한다.
- 이미지 권한 거절·선택 실패: 폼을 유지하고 원인을 안내한다.
- 중복 오류: 사업자등록번호 입력 하단에 오류를 표시한다.
- 이메일 중복·비밀번호 정책 오류: 해당 입력 하단에 오류를 표시한다.
- 저장 오류: 입력 내용을 유지하고 화면 상단에 재시도 가능한 오류를 표시한다.
- 저장 성공: 업체 목록 캐시를 갱신한 후 업체 목록으로 교체 이동한다.

### ADM-007 업체 상세

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(admin)/partners/[partnerId].tsx` |
| 경로 | `/(admin)/partners/[partnerId]` |
| 대상 역할 | 관리자 |
| 목적 | 목록에서 선택한 업체의 등록 정보를 읽기 전용으로 확인한다. |
| 데이터 | Supabase `partners`, 관리자 전용 `partner_login_accounts`, 사업자등록증 단기 서명 URL |
| 주요 표시 | 업체명, 사업자등록번호, 업체 대표 로그인 이메일, 담당자 이름, 담당자 연락처, 사업자등록증 이미지 또는 미첨부 안내, 비고 또는 미입력 안내, 등록일 |

| 액션 | 조건 | 결과 |
|---|---|---|
| 뒤로 | 항상 | 업체 목록으로 돌아간다. |
| 다시 시도 | 상세 조회 실패 | 같은 업체를 다시 조회한다. |

상태:

- 로딩: 중앙 로딩 표시와 업체 정보를 불러오는 중이라는 문구를 표시한다.
- 없음·권한 제한: 업체를 찾을 수 없다는 안내와 목록 이동 버튼을 표시한다.
- 조회 오류: 네트워크 확인 안내와 `다시 시도` 버튼을 표시한다.
- 데이터 있음: 모든 입력값을 읽기 전용 섹션으로 표시한다.

현재 제약:

- 수정 기능은 제공하지 않는다.
- 사업자등록증 원본 다운로드·공유 기능은 제공하지 않는다.
- 비밀번호는 어떤 상세 화면에서도 조회하거나 표시하지 않는다.

### ADM-005 견적 옵션 목록

| 항목 | 정의 |
|---|---|
| 상태 | `구현` |
| 구현 파일 | `app/(admin)/catalog/index.tsx`, `src/features/quote-option-management/` |
| 경로 | `/(admin)/catalog` |
| 대상 역할 | 관리자 |
| 목적 | 고객 견적양식에 사용할 상위 카테고리를 표시 순서대로 확인한다. |
| 데이터 | Supabase `quote_option_masters`, `quote_option_products` |
| 주요 표시 | 총 카테고리 수, 코드, 이름, 단순형·고급형 타입, 등록 제품 수 |

| 액션 | 조건 | 결과 |
|---|---|---|
| 뒤로 | 항상 | 관리자 운영 홈으로 돌아간다. |
| 새로고침 | 항상 | 최신 견적 옵션 마스터 목록을 다시 조회한다. |
| 옵션 선택 | 목록 행이 존재 | 선택한 `/(admin)/catalog/[optionId]`로 이동한다. |
| 다시 시도 | 목록 조회 실패 | 목록을 다시 조회한다. |

초기 마스터:

| 순서 | 코드 | 이름 | 타입 |
|---:|---|---|---|
| 1 | `TILE` | 타일 | 고급형 |
| 2 | `BASIN_TOILET` | 세면대/양변기 | 단순형 |
| 3 | `WATER_TANK` | 수조 | 단순형 |
| 4 | `BATHTUB` | 욕조 | 단순형 |
| 5 | `ZENDAI` | 젠다이 | 단순형 |
| 6 | `MASONRY_WALL` | 조적벽 | 단순형 |
| 7 | `CEILING` | 천장제 | 단순형 |
| 8 | `BATHROOM_CABINET` | 욕실장 | 단순형 |
| 9 | `INDIRECT_LIGHTING` | 간접등 |

상태:

- 로딩: 옵션 목록을 불러오는 중이라는 문구를 표시한다.
- 빈 상태: 등록된 견적 옵션이 없다는 안내를 표시한다.
- 데이터 있음: `display_order`, 이름 순으로 한 항목씩 세로 목록에 표시한다.
- 각 목록 항목은 공통 카테고리 아이콘, 이름·코드, 타입, 등록 제품 수, 상세 이동 표시를
  하나의 가로 행에 표시하며 순서 숫자는 별도로 노출하지 않는다.
- 타입 배지는 단순형을 중립 회색, 고급형을 브랜드 청록색으로 구분하며 텍스트 라벨을 함께
  표시한다.
- 조회 오류: 네트워크 확인 안내와 `다시 시도` 버튼을 표시한다.

### ADM-008 견적 옵션 상세·수정

| 항목      | 정의                                                                                           |
| --------- | ---------------------------------------------------------------------------------------------- |
| 상태      | `구현`                                                                                         |
| 구현 파일 | `app/(admin)/catalog/[optionId].tsx`, `src/features/quote-option-management/` |
| 경로 | `/(admin)/catalog/[optionId]` |
| 대상 역할 | 관리자 |
| 목적 | 견적 카테고리의 고객 노출 정보와 그 안에 속하는 제품을 수정한다. |
| 데이터 | Supabase `quote_option_masters`, `quote_option_products`, 비공개 Storage `quote-option-images` |
| 주요 표시 | 읽기 전용 코드, 옵션명, 표시 순서, 단순형·고급형 타입, 복수 제품의 순서·이름·이미지·단가 |

| 액션 | 조건 | 결과 |
|---|---|---|
| 뒤로 | 저장 중이 아님 | 견적 옵션 목록으로 돌아간다. |
| 제품 추가 | 저장 중이 아님 | 순서·이름·이미지·단가 입력을 포함하는 새 제품 묶음을 추가한다. |
| 제품 삭제 | 제품이 존재하고 저장 중이 아님 | 선택한 제품 묶음과 해당 이미지를 저장 대상에서 제거한다. |
| 제품 이미지 추가·변경 | 저장 중이 아님 | 사진 접근 권한을 요청하고 해당 제품의 이미지 한 장을 미리보기에 설정한다. |
| 변경사항 저장 | 카테고리 속성이 유효하고 저장 중이 아니며, 등록된 제품이 있다면 모든 제품의 이름·이미지·단가가 유효함 | 제품이 0개인 상태를 포함해 카테고리와 제품 구성을 저장하고 최신 상세·목록 캐시를 갱신한다. |
| 다시 시도 | 상세 조회 실패 | 같은 옵션을 다시 조회한다. |

규칙:

- 코드는 대문자 영문과 밑줄로 구성된 변경 불가 식별자다.
- 옵션명은 필수이며 50자 이하, 표시 순서는 1 이상 9,999 이하의 정수다.
- 9개의 초기 마스터는 카테고리다. 각 카테고리에는 제품을 0개 이상 추가할 수 있으며, 등록된 제품은 이름·이미지·단가를 한 묶음으로 가진다.
- 제품 순서는 숫자만 입력할 수 있고 정수로 저장한다. 중복 순서를 허용하며 별도 유효성
  검사를 적용하지 않고, 빈 값과 새 제품의 기본값은 `0`으로 저장한다.
- 제품명은 필수이며 100자 이하, 제품 단가는 원 단위 0원 이상 1조 원 이하의 정수로 저장하고 입력 중 1,000단위 콤마를 표시한다.
- 등록하는 모든 제품에는 이미지 한 장이 필수다. 이미지는 JPEG·PNG·HEIC·HEIF를 지원하고 이미지당 최대 10MB다.
- 이미지는 관리자 화면에서 변경한다. MVP 단계에서는 개발 편의를 위해 비공개
  `quote-option-images` 버킷의 읽기·추가·삭제를 모든 인증 사용자에게 허용하며, 정식 운영 전
  관리자 전용 Storage 정책으로 강화한다.
- `TILE`은 최초 `advanced`, 나머지는 최초 `simple`로 저장한다. 1차 상세 폼은 두 타입이
  동일하며 고급형 전용 입력폼은 후속 범위다.
- 비활성 항목은 향후 신규 고객 선택에서 제외하지만 기존 요청 스냅샷에는 유지한다.
- 향후 고객 요청에는 카테고리 ID뿐 아니라 선택 제품 ID·이름·이미지 경로·단가를 제출 시점 스냅샷으로 보존한다.

상태:

- 로딩: 옵션 상세를 불러오는 중이라는 문구를 표시한다.
- 없음·권한 제한: 옵션을 찾을 수 없다는 안내와 목록 이동 버튼을 표시한다.
- 제품 공란: 등록된 제품이 없다는 안내와 제품 추가 버튼을 표시한다.
- 검증 오류: 해당 입력 아래에 오류 문구를 표시하고 저장하지 않는다.
- 저장 중: 입력·이미지·이동·저장 액션을 비활성화한다.
- 저장 성공: 완료 안내를 표시하고 수정된 값을 유지한다.
- 저장 실패: 입력 내용을 유지하고 재시도 가능한 오류를 표시한다.

현재 제약:

- 신규 마스터 추가·삭제·활성/비활성 전환은 제공하지 않는다.
- 제품은 제품 순서 오름차순으로 표시한다. 같은 순서에서는 생성일시, ID 오름차순으로
  안정적으로 정렬하며 기존 제품의 제품 순서는 모두 `0`으로 초기화한다.
- 고급형 타일 전용 입력 필드는 후속 범위다. 현재 고객 요청 화면은 단순형·고급형 모두 동일한 제품 갤러리로 표시하고 제품별 단가를 합산한다.
- Storage 이미지 변경 권한은 MVP 개발 편의를 위해 인증 사용자 단위로 완화되어 있다.

## 8. 공통 화면·기능 규칙

### 8.1 내비게이션과 권한

- 비로그인 사용자는 보호 화면에 진입할 수 없다.
- 로그인 사용자는 역할에 허용된 화면만 열 수 있다.
- 역할 권한은 화면 숨김만으로 처리하지 않고 서버 데이터 정책과 함께 검증한다.
- 상세 화면에서 대상 데이터가 없거나 권한이 없으면 명시적인 대체 화면을 제공한다.
- Android 역할별 최상위 화면에서 하드웨어 뒤로가기를 처음 누르면 앱을 종료하지 않고 `한번 더 뒤로가기를 눌러 앱을 종료할 수 있어요` 토스트를 2초 동안 표시한다. 같은 2초 안에 다시 누른 경우에만 앱을 종료하며, 시간이 지나면 다음 입력부터 같은 안내 절차를 다시 시작한다.

### 8.2 화면 상태

서버 데이터를 사용하는 화면은 다음 상태를 반드시 정의한다.

- 초기 로딩
- 데이터 있음
- 데이터 없음
- 입력 검증 실패
- 네트워크 또는 서버 오류
- 재시도
- 저장 또는 전송 중
- 저장 또는 전송 완료

해당하지 않는 상태는 화면 정의에서 `해당 없음`과 이유를 기록한다.

### 8.3 접근성과 레이아웃

- 모든 화면에 `SafeAreaView` 또는 동등한 안전 영역 처리를 적용한다.
- 모든 터치 요소는 최소 44px 크기를 확보한다.
- 버튼에는 보이는 문구 또는 `accessibilityLabel`을 제공한다.
- 상태는 색상만으로 구분하지 않고 텍스트를 함께 표시한다.
- 처리 중인 버튼은 중복 실행되지 않도록 비활성화한다.

### 8.4 데이터와 보안

- 인증 토큰과 세션은 SecureStore 기반 저장소만 사용한다.
- 화면과 UI 컴포넌트에서 Supabase를 직접 조회하지 않는다.
- 고객 요청·견적·메시지의 서버 연결은 feature repository와 TanStack Query를 사용한다.
- 서비스 역할 키와 비밀값을 앱 번들에 포함하지 않는다.
- 고객 견적 사진은 원격 저장이 제품 범위로 승인되기 전까지 로컬 전용으로 유지한다.
- 업체 사업자등록증은 승인된 범위이며 관리자 전용 비공개 Storage에 저장한다.

## 9. 화면 변경 관리 절차

### 9.1 구현 전

1. 요청이 영향을 주는 역할과 화면 ID를 찾는다.
2. 기존 목적, 액션, 상태, 권한, 이동 경로와 요청 내용을 비교한다.
3. 정의가 없으면 새 화면 ID와 정의를 먼저 추가한다.
4. 기존 정의와 충돌하면 사용자 결정을 확인하고 문서를 먼저 갱신한다.
5. 구현 대상과 비대상을 확정한다.

### 9.2 구현 후

1. 실제 라우트와 구현 파일을 기록한다.
2. 버튼 문구, 조건, 결과, 이동 경로를 코드와 대조한다.
3. 로딩·빈 상태·오류·완료 상태를 대조한다.
4. 서버 연동인지 로컬 상태인지 정확하게 표시한다.
5. 역할별 화면 요약과 상세 정의를 함께 갱신한다.
6. 문서 검증과 프로젝트 정적 검사를 실행한다.

### 9.3 변경 완료 조건

- 변경된 모든 화면이 이 문서에 반영되어 있다.
- 새 화면에는 중복되지 않는 화면 ID가 있다.
- 모든 실제 화면 파일이 이 문서에서 추적된다.
- 구현하지 않은 기능을 `구현`으로 표시하지 않는다.
- 문서와 코드가 다르면 작업을 완료로 보고하지 않는다.

## 10. 변경 이력

| 날짜 | 문서 버전 | 변경 내용 |
|---|---|---|
| 2026-07-30 | 2.5 | 고객·업체 담당자·관리자 최상위 화면의 Android 하드웨어 뒤로가기 2초 이중 입력 종료 보호 정의 |
| 2026-07-29 | 2.4 | 고객 설정의 로그인 정보 상세 화면과 이름·연락처 Supabase 조회·수정 정의 |
| 2026-07-29 | 2.3       | 업체·관리자 시공중 견적의 `시공 완료` 액션과 `closed` 완료 상태 전이 정의                                      |
| 2026-07-29 | 2.2 | 관리자 견적 조정을 신규 견적 `submitted` 상태의 최초 1회로 제한 |
| 2026-07-29 | 2.1 | 관리자 견적 상세의 등록 업체·대표 담당자 선택 모달과 원자적 업체 배정 정의 |
| 2026-07-29 | 2.0 | 업체 배정 견적 4개 탭·상세 진행/취소 응답과 `cancelled` 종료 상태 정의 |
| 2026-07-28 | 1.9 | 개발 모드 로그인 화면에 고객·업체·관리자 임시 계정 자동 입력 배지 정의 |
| 2026-07-27 | 1.8 | 옵션 제품을 생성일시 오름차순으로 표시하도록 정의 |
| 2026-07-27 | 1.7 | 카테고리의 모든 제품 삭제 후 빈 상태를 저장할 수 있도록 명시 |
| 2026-07-27 | 1.6 | 견적 옵션 9종을 카테고리로 전환하고, 카테고리별 복수 제품(이름·이미지·단가) 관리 정의 |
| 2026-07-27 | 1.5 | 견적 옵션 9종 마스터, 목록, 상세 수정, 단가·타입·순서와 최대 5장 이미지 관리 정의 |
| 2026-07-27 | 1.4 | 관리자 홈을 견적 및 시공 관리·운영 관리 섹션으로 재구성하고 견적 관리·업체 관리·견적 옵션 관리 3개 메뉴로 정리 |
| 2026-07-27 | 1.3 | 업체 등록 시 대표 담당자 Auth 생성·연결, 로그인 이메일·비밀번호 필수 입력과 업체 담당자 홈 범위 정의 |
| 2026-07-27 | 1.2 | 관리자 업체 목록·추가·상세와 Supabase 저장, 사업자등록번호 중복 검사, 비공개 증빙 이미지 구현 |
| 2026-07-27 | 1.1 | 관리자 인증 허용, 역할별 라우팅, 관리자 운영 홈 셸과 운영 계정 정책 추가 |
| 2026-07-27 | 1.0 | 단일 정본 생성, 현재 공통·고객 화면 구현 조사, 업체 담당자·관리자 계획 통합 |
